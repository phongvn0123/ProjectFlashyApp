/// Route path constants — the single source of truth for route paths.
///
/// Features (Phases 2-6) MUST import these instead of hardcoding path
/// strings, so the 5-tab shell contract stays consistent across all
/// developers.
library;

const String kHomeRoute = '/home';
const String kLibraryRoute = '/library';
const String kClassroomRoute = '/classroom';
const String kQuizRoute = '/quiz';
const String kProfileRoute = '/profile';
const String kLoginRoute = '/auth/login';
