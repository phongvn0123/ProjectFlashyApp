import 'package:flutter/material.dart';

/// Placeholder for the "Bài kiểm tra" (Quiz) tab.
///
/// Replaced by Phase 6 (Quiz/Test) — see SKELETON.md Subsequent Slice Plan.
class QuizPlaceholderPage extends StatelessWidget {
  const QuizPlaceholderPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Bài kiểm tra')),
      body: const Center(child: Text('Bài kiểm tra — sắp ra mắt')),
    );
  }
}
