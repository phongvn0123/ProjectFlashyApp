import 'package:flutter/material.dart';

/// Placeholder for the "Lớp học" (Classroom) tab.
///
/// Replaced by Phase 5 (Classroom) — see SKELETON.md Subsequent Slice Plan.
class ClassroomPlaceholderPage extends StatelessWidget {
  const ClassroomPlaceholderPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Lớp học')),
      body: const Center(child: Text('Lớp học — sắp ra mắt')),
    );
  }
}
