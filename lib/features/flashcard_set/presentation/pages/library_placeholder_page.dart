import 'package:flutter/material.dart';

/// Placeholder for the "Thư viện" (Library) tab.
///
/// Replaced by Phase 3 (Flashcard Set) — see SKELETON.md Subsequent Slice
/// Plan.
class LibraryPlaceholderPage extends StatelessWidget {
  const LibraryPlaceholderPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Thư viện')),
      body: const Center(child: Text('Thư viện — sắp ra mắt')),
    );
  }
}
