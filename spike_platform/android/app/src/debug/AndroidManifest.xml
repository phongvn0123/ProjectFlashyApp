<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The INTERNET permission is required for development. Specifically,
         the Flutter tool needs it to communicate with the running application
         to allow setting breakpoints, to provide hot reload, etc.
    -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- Debug-only: allow cleartext (unencrypted) traffic so the app can reach
         the Firebase Emulator Suite via the Android emulator's loopback alias
         10.0.2.2. NEVER add this attribute to src/main/AndroidManifest.xml or
         any release build config (per 00-RESEARCH.md Pitfall 2 / threat T-0-01).
    -->
    <application android:usesCleartextTraffic="true" />
</manifest>
