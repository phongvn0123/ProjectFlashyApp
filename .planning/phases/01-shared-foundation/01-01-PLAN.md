---
phase: 01-shared-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - analysis_options.yaml
  - android/app/build.gradle.kts
  - android/app/src/main/AndroidManifest.xml
  - android/build.gradle.kts
  - android/settings.gradle.kts
  - .gitignore
  - lib/main.dart
  - lib/core/theme/app_theme.dart
  - lib/core/theme/colors.dart
  - lib/core/router/router.dart
  - lib/core/router/routes.dart
  - lib/core/widgets/bottom_nav_shell.dart
  - lib/features/home/presentation/pages/home_placeholder_page.dart
  - lib/features/flashcard_set/presentation/pages/library_placeholder_page.dart
  - lib/features/classroom/presentation/pages/classroom_placeholder_page.dart
  - lib/features/quiz/presentation/pages/quiz_placeholder_page.dart
  - lib/features/auth/presentation/pages/profile_placeholder_page.dart
  - lib/features/auth/presentation/pages/login_placeholder_page.dart
  - test/core/theme/app_theme_test.dart
  - test/core/router/router_test.dart
  - integration_test/app_test.dart
autonomous: true
requirements: [FND-08, FND-09]

must_haves:
  truths:
    - "A developer runs `flutter run -d emulator-5554` and sees the real Memocard app, not the Flutter counter demo or the Phase 0 spike"
    - "All 5 tabs (Trang chủ, Thư viện, Lớp học, Bài kiểm tra, Cá nhân) are tappable and each shows a distinct placeholder screen"
    - "Switching tabs preserves per-tab navigation state (StatefulShellRoute.indexedStack, not a full rebuild)"
    - "The app renders in the Academic Precision color palette (Action Blue #0066cc primary, #f9f9ff light surface) — not Flutter's default Material purple"
    - "flutter analyze is clean and flags any BuildContext-after-await misuse via use_build_context_synchronously"
  artifacts:
    - path: "pubspec.yaml"
      provides: "Real project manifest with all CLAUDE.md-mandated + Phase-0-corrected dependency versions"
      contains: "sqflite: ^2.4.2"
    - path: "lib/core/theme/app_theme.dart"
      provides: "AppTheme.light() / AppTheme.dark() ThemeData built from Academic Precision tokens"
      exports: ["AppTheme"]
    - path: "lib/core/router/router.dart"
      provides: "routerProvider (Provider<GoRouter>) with StatefulShellRoute 5-tab shell"
      exports: ["routerProvider"]
    - path: "lib/core/widgets/bottom_nav_shell.dart"
      provides: "BottomNavShell — 88px NavigationBar wired to StatefulNavigationShell"
      exports: ["BottomNavShell"]
  key_links:
    - from: "lib/main.dart"
      to: "lib/core/router/router.dart"
      via: "ProviderScope + MaterialApp.router(routerConfig: ref.watch(routerProvider))"
      pattern: "routerProvider"
    - from: "lib/core/router/router.dart"
      to: "lib/core/widgets/bottom_nav_shell.dart"
      via: "StatefulShellRoute.indexedStack builder"
      pattern: "BottomNavShell"
---

<objective>
Establish the real Memocard Flutter project at the repo root and ship a runnable 5-tab app shell with the Academic Precision theme applied — the walking skeleton's visible surface. This is the ONLY plan in Wave 1: it has zero dependencies and must not be blocked by Firebase, SQLite schema, Firestore rules, the base repository, or team docs (all deferred to later waves per the explicit Phase 1 planning directive).

**After this plan, `flutter run -d emulator-5554` shows a real, navigable Memocard app** — 5 tabs, light/dark Academic Precision theme, placeholder screens per tab. No login flow, no data, no Firebase yet — those come in Waves 2–4.

Purpose: give the user (frustrated after a throwaway Phase 0 spike with no visible product) something real to look at immediately, and give the other 4 developers a locked shell contract (`lib/core/router/`, `lib/core/theme/`) to build their tabs against in Phases 2–6.
Output: A `flutter create`-scaffolded project at repo root with `lib/core/theme/`, `lib/core/router/`, `lib/core/widgets/bottom_nav_shell.dart`, 6 placeholder pages, and `spike_platform/` removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-shared-foundation/01-RESEARCH.md
@.planning/reference/ui-screens/academic_precision/DESIGN.md
@.planning/phases/00-platform-spike/00-SPIKE-FINDINGS.md
@spike_platform/pubspec.yaml
@spike_platform/android/app/build.gradle.kts
@spike_platform/lib/main.dart
</context>

<interfaces>
<!-- 01-RESEARCH.md § "Theme: Academic Precision Light/Dark Implementation" (~line 1074-1247) already contains a working AppTheme.light()/dark() implementation whose hex values were cross-checked against DESIGN.md front-matter and match (surface #f9f9ff, on-surface #191c22, inverse-surface #2e3037 used for dark surface, inverse-on-surface #eff0f9 used for dark on-surface, error #ba1a1a, primary #0066cc = DESIGN.md's "Action Blue"). Use that code directly as the base implementation, adjusted per the acceptance criteria below (button/input/card radii, ThemeMode.system wiring). -->

<!-- 01-RESEARCH.md § "GoRouter 17.x: 5-Tab Bottom Nav Shell with Auth Guard" (~line 694-833) contains a working StatefulShellRoute.indexedStack skeleton. Reuse its structure but: (a) DO NOT add the `redirect` auth-guard logic yet — Wave 1 has no authStateProvider (Firebase doesn't exist yet); leave `redirect: (context, state) => null` with a `// TODO(01-05): wire authStateProvider redirect once core providers land` comment; (b) tab paths/labels/icons must match the Vietnamese labels below, not the English placeholders in the research doc. -->

Route → Vietnamese label → icon → placeholder widget (in this exact order, index 0-4):
0. `/home` → "Trang chủ" → `Icons.home_rounded` → `HomePlaceholderPage`
1. `/library` → "Thư viện" → `Icons.menu_book_rounded` → `LibraryPlaceholderPage`
2. `/classroom` → "Lớp học" → `Icons.school_rounded` → `ClassroomPlaceholderPage`
3. `/quiz` → "Bài kiểm tra" → `Icons.fact_check_rounded` → `QuizPlaceholderPage`
4. `/profile` → "Cá nhân" → `Icons.person_rounded` → `ProfilePlaceholderPage`

Additional route outside the shell: `/auth/login` → `LoginPlaceholderPage` (no logic, just a placeholder Scaffold — Phase 2 replaces this). `GoRouter.initialLocation = '/home'` for Wave 1 (no auth gating yet).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the real Flutter project, pin validated dependencies, delete spike_platform</name>
  <files>pubspec.yaml, analysis_options.yaml, android/app/build.gradle.kts, android/app/src/main/AndroidManifest.xml, android/build.gradle.kts, android/settings.gradle.kts, .gitignore, integration_test/app_test.dart</files>
  <read_first>spike_platform/pubspec.yaml, spike_platform/android/app/build.gradle.kts, .planning/phases/00-platform-spike/00-SPIKE-FINDINGS.md, .planning/phases/01-shared-foundation/01-RESEARCH.md (§ Standard Stack, § Installation Verification)</read_first>
  <action>
Run `flutter create --org com.memocard --project-name memocard --platforms=android .` at the repo root (this repo root — NOT a new subfolder). This generates `lib/main.dart` (default counter app, overwritten by Task 3), `android/`, `test/widget_test.dart`, a fresh `.gitignore`, and `pubspec.yaml` at repo root, alongside the existing `.planning/`, `CLAUDE.md`, `spike_platform/`.

Delete `spike_platform/` entirely (its findings are fully captured in `00-SPIKE-FINDINGS.md` and `STATE.md`; keeping a second `pubspec.yaml`/Android project at repo root risks tooling confusion). This is the explicit decision for this phase — do not preserve it "for later cleanup".

Edit the generated `pubspec.yaml`: set `environment: sdk: ^3.11.5` (matches Flutter 3.41.9's bundled Dart, per Phase 0 finding — NOT the CLAUDE.md-implied ^3.5.0 floor, which is too permissive and would let `sqflite ^2.4.3` resolve and break). Add to `dependencies`: `flutter_riverpod: ^3.3.2`, `riverpod_annotation: ^4.0.3`, `sqflite: ^2.4.2` (NOT `^2.4.3` — CRITICAL, see Phase 0 finding: 2.4.3 requires Dart SDK ^3.12.0, unavailable), `firebase_core: ^4.12.1`, `firebase_auth: ^6.5.6`, `cloud_firestore: ^6.7.1`, `shared_preferences: ^2.5.5`, `go_router: ^17.3.0`, `freezed_annotation: ^2.4.4`, `json_serializable: ^6.14.0` (dependency, not dev — models need it at runtime via generated code... actually keep `json_annotation: ^4.9.0` as the runtime dep, `json_serializable` and `freezed` are codegen and belong in `dev_dependencies`), `json_annotation: ^4.9.0`. Add to `dev_dependencies`: `riverpod_generator: ^4.0.4`, `freezed: ^3.2.5`, `build_runner: ^2.15.2`, `flutter_lints: ^6.0.0`, `integration_test: {sdk: flutter}`.

Create `integration_test/app_test.dart` as a placeholder smoke test (will be extended by later plans in this phase): pumps the app entrypoint, asserts it builds without throwing. This satisfies 01-VALIDATION.md's Wave 0 requirement for `integration_test/` + dev dependency to exist before later plans add real SQLite/Firebase integration tests.

Edit `android/app/build.gradle.kts`: `namespace = "com.memocard.memocard"`, `applicationId = "com.memocard.memocard"`, `minSdk = 24` (explicit, with a comment citing the Phase 0 finding that Flutter's `MinSdkVersionMigration` silently reverts any value in range 16-23 back to `flutter.minSdkVersion`, which is already 24 — do not set 23). Do **NOT** add the `com.google.gms.google-services` plugin or any Firebase Gradle config in this task — that is deferred to the human checkpoint in 01-04-PLAN.md and the emulator-only wiring in 01-05-PLAN.md; Wave 1 must build and run with zero Firebase dependency. Set `android:label="Memocard"` in `android/app/src/main/AndroidManifest.xml`.

Edit `analysis_options.yaml`: under `linter: rules:`, explicitly add `use_build_context_synchronously: true` (do not rely on `flutter_lints`' default set including it — make it explicit and auditable, with a comment: `# Phase 0 code review finding: setState()/BuildContext use after an unguarded await is the #1 bug class to prevent across all 5 developers. This lint plus the pattern demonstrated in ProfilePlaceholderPage (Task 3) is the Wave 1 mitigation — see DEVELOPER_GUIDE.md.`).

Append to `.gitignore` (flutter create's generated one already covers `build/`, `.dart_tool/`): `**/google-services.json`, `firebase-debug.log`, `firestore-debug.log`, `ui-debug.log`, `.firebase/` — these will be produced by Waves 2-3 and must never be committed.

Run `flutter pub get`. Then run `flutter pub deps | grep -i sqflite` and confirm the resolved version line shows `sqflite 2.4.2` (not 2.4.3) — if it shows 2.4.3 or fails to resolve, the pin in pubspec.yaml is wrong and must be fixed before continuing to any other task in this phase.
  </action>
  <verify>
    <automated>cd "C:\Users\LENOVO\AndroidStudioProjects\prm_proejct" && flutter pub get && flutter pub deps | grep -i "sqflite 2.4.2" && flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - `spike_platform/` directory no longer exists (`git status` shows it as deleted, not untracked)
    - `pubspec.yaml` exists at repo root (same level as `CLAUDE.md`), `name: memocard`
    - `flutter pub deps | grep -i sqflite` output contains the exact substring `sqflite 2.4.2` and does NOT contain `sqflite 2.4.3`
    - `android/app/build.gradle.kts` contains `applicationId = "com.memocard.memocard"` and `minSdk = 24`, and does NOT contain `com.google.gms.google-services`
    - `analysis_options.yaml` contains the literal line `use_build_context_synchronously: true`
    - `flutter analyze` reports `No issues found!`
    - `integration_test/app_test.dart` exists
  </acceptance_criteria>
  <done>Real Flutter project scaffolded at repo root with validated dependency pins, Android config, lint rule, and spike_platform removed; flutter pub get and flutter analyze both succeed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the Academic Precision light/dark theme</name>
  <files>lib/core/theme/app_theme.dart, lib/core/theme/colors.dart, test/core/theme/app_theme_test.dart</files>
  <read_first>.planning/reference/ui-screens/academic_precision/DESIGN.md, .planning/phases/01-shared-foundation/01-RESEARCH.md (§ 7 Theme, ~line 1074-1247)</read_first>
  <behavior>
    - `AppTheme.light().colorScheme.brightness` equals `Brightness.light`; `AppTheme.dark().colorScheme.brightness` equals `Brightness.dark`
    - `AppTheme.light().colorScheme.primary` equals `Color(0xFF0066CC)` (DESIGN.md `primary-container` token, described in DESIGN.md prose as "Action Blue", reserved for buttons/links/active states)
    - `AppTheme.light().colorScheme.error` equals `Color(0xFFBA1A1A)` and `AppTheme.dark().colorScheme.error` equals the same (DESIGN.md `error` token, same value both themes)
    - `AppTheme.light().elevatedButtonTheme.style` produces a fully pill-shaped button: `RoundedRectangleBorder.borderRadius` resolves to a radius `>= 100` (DESIGN.md "all buttons must be fully pill-shaped, 100px or higher radius")
    - `AppTheme.light().useMaterial3` and `AppTheme.dark().useMaterial3` are both `true`
  </behavior>
  <action>
Create `lib/core/theme/colors.dart` holding every DESIGN.md front-matter color token as a named `Color` constant (e.g. `kSurface = Color(0xFFF9F9FF)`, `kPrimaryContainer = Color(0xFF0066CC)`, `kError = Color(0xFFBA1A1A)`, etc. — transcribe the full 30+ token list from DESIGN.md front-matter verbatim, hex-to-ARGB).

Create `lib/core/theme/app_theme.dart` with `AppTheme.light()` and `AppTheme.dark()` static methods returning `ThemeData`, following 01-RESEARCH.md's already-DESIGN.md-consistent example code as the base: `useMaterial3: true`, `ColorScheme.light`/`ColorScheme.dark` built from `colors.dart` constants (primary = Action Blue `#0066cc`, error = `#ba1a1a`, light surface = `#f9f9ff`/on-surface `#191c22`, dark surface = inverse-surface `#2e3037`/on-surface = inverse-on-surface `#eff0f9`), `textTheme` populated per DESIGN.md typography tokens (`display-lg` 34/700/1.1 line-height/-0.02em letter-spacing, `headline-md` 24/600/1.2/-0.01em, `headline-sm` 20/600/1.2, `body-lg` 17/400/1.4, `body-md` 15/400/1.4, `label-lg` 13/600/1.2, `label-md` 12/500/1.2), all with `fontFamily: 'Inter'`. Note in a code comment: no Inter `.ttf` asset is bundled in Phase 1 (none provided in the repo), so this falls back to the platform default (Roboto on Android) — acceptable per SKELETON.md's documented scope, not a defect; bundling the real font is a later cosmetic pass if desired.

`elevatedButtonTheme`: pill shape via `BorderRadius.circular(999)` (DESIGN.md: "100px or higher radius"), `minimumSize: Size(double.infinity, 48)`. `cardTheme`: `RoundedRectangleBorder(borderRadius: BorderRadius.circular(18))` (DESIGN.md Components § Product Tiles / Cards: "18px corner radius", NOT the generic `rounded.lg` token of 32px — the concrete component spec overrides the abstract token scale). `inputDecorationTheme`: `BorderRadius.circular(12)` (DESIGN.md: "Search and text fields use a 12px corner radius").
  </action>
  <verify>
    <automated>cd "C:\Users\LENOVO\AndroidStudioProjects\prm_proejct" && flutter test test/core/theme/app_theme_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `test/core/theme/app_theme_test.dart` exists and contains 5+ `test()` blocks covering every assertion in `<behavior>` above
    - `flutter test test/core/theme/app_theme_test.dart` passes with 0 failures
    - `lib/core/theme/app_theme.dart` exports exactly `AppTheme` with two public static members `light()` and `dark()`
  </acceptance_criteria>
  <done>AppTheme.light()/dark() exist, match DESIGN.md's Academic Precision tokens (verified by unit test), and are ready for main.dart to consume.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the 5-tab GoRouter shell with placeholder screens and apply the theme</name>
  <files>lib/main.dart, lib/core/router/router.dart, lib/core/router/routes.dart, lib/core/widgets/bottom_nav_shell.dart, lib/features/home/presentation/pages/home_placeholder_page.dart, lib/features/flashcard_set/presentation/pages/library_placeholder_page.dart, lib/features/classroom/presentation/pages/classroom_placeholder_page.dart, lib/features/quiz/presentation/pages/quiz_placeholder_page.dart, lib/features/auth/presentation/pages/profile_placeholder_page.dart, lib/features/auth/presentation/pages/login_placeholder_page.dart, test/core/router/router_test.dart</files>
  <read_first>.planning/phases/01-shared-foundation/01-RESEARCH.md (§ 5 GoRouter 17.x, ~line 694-833), .planning/reference/ui-screens/academic_precision/DESIGN.md (§ Navigation component: "88px height, icons 24px, inkMuted48 inactive, Action Blue active")</read_first>
  <action>
Create `lib/core/router/routes.dart` with route path string constants (`kHomeRoute = '/home'`, `kLibraryRoute = '/library'`, `kClassroomRoute = '/classroom'`, `kQuizRoute = '/quiz'`, `kProfileRoute = '/profile'`, `kLoginRoute = '/auth/login'`) — this is the single source of truth other plans/features will import instead of hardcoding path strings.

Create `lib/core/router/router.dart` exporting `final routerProvider = Provider<GoRouter>((ref) => ...)` per the `<interfaces>` block above: `initialLocation: kHomeRoute`, `redirect: (context, state) => null` with the `TODO(01-05)` comment, one `StatefulShellRoute.indexedStack` with exactly 5 `StatefulShellBranch`es in the order specified in `<interfaces>`, plus a top-level `GoRoute(path: kLoginRoute, builder: (_, __) => const LoginPlaceholderPage())` outside the shell.

Create `lib/core/widgets/bottom_nav_shell.dart`: `BottomNavShell extends ConsumerWidget` taking `StatefulNavigationShell navigationShell`, `Scaffold(body: navigationShell, bottomNavigationBar: NavigationBar(height: 88, selectedIndex: navigationShell.currentIndex, onDestinationSelected: navigationShell.goBranch, destinations: [...5 NavigationDestination per <interfaces> icons/labels...]))`.

Create the 6 placeholder pages as simple `Scaffold(appBar: AppBar(title: Text('<Vietnamese label>')), body: Center(child: Text('<Vietnamese label> — sắp ra mắt')))`. Make `ProfilePlaceholderPage` a `StatefulWidget` that on `initState` kicks off `Future.delayed(const Duration(milliseconds: 300))` then calls `setState` — **guarded by `if (!mounted) return;` immediately before the `setState` call**, with a comment `// Canonical pattern: never call setState() after an await without checking mounted first. See DEVELOPER_GUIDE.md.` This is the concrete Wave 1 reference implementation of the Phase 0 code-review finding (setState-after-await without mounted check).

Rewrite `lib/main.dart`: `void main() { runApp(const ProviderScope(child: MemocardApp())); }` and `MemocardApp extends ConsumerWidget` reading `ref.watch(routerProvider)`, returning `MaterialApp.router(routerConfig: router, theme: AppTheme.light(), darkTheme: AppTheme.dark(), themeMode: ThemeMode.system, title: 'Memocard', debugShowCheckedModeBanner: false)`. No Firebase, no SharedPreferences, no async main() — this task must not introduce any dependency Wave 2/3 hasn't built yet.

Delete or overwrite the default `test/widget_test.dart` (references the deleted counter-app `MyApp` — leaving it will break `flutter analyze`/`flutter test`, same Phase 0 finding).
  </action>
  <verify>
    <automated>cd "C:\Users\LENOVO\AndroidStudioProjects\prm_proejct" && flutter test test/core/router/router_test.dart && flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - `test/core/router/router_test.dart` pumps `MemocardApp` wrapped in `ProviderScope`, finds all 5 `NavigationDestination` labels (Trang chủ, Thư viện, Lớp học, Bài kiểm tra, Cá nhân), taps each, and asserts the corresponding placeholder page's title text becomes visible — this test passes
    - `flutter analyze` reports `No issues found!` (no stale `MyApp` reference, no unused imports)
    - `grep -rn "if (!mounted) return" lib/features/auth/presentation/pages/profile_placeholder_page.dart` returns a match
    - Manual: `flutter run -d emulator-5554` launches the app, all 5 tabs are tappable and show distinct Vietnamese-labeled placeholder content, the app bar/buttons render in Action Blue (#0066cc), not Material default purple
  </acceptance_criteria>
  <done>App launches into the real 5-tab Memocard shell with Academic Precision theme applied; router_test.dart proves all 5 tabs navigate correctly; the mounted-guard pattern is established in code for the other 4 developers to copy.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Local device only | Wave 1 has no network calls, no Firebase, no untrusted input — pure UI shell. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-01-00 | Tampering | pubspec.yaml dependency pins | accept | All 12 packages already verified in 01-RESEARCH.md's Package Legitimacy Audit table (Google/official ecosystem, 100M+ cumulative downloads, all Approved) — no new package introduced in this plan, no checkpoint needed |
| T-01-01 | Information Disclosure | android/app/build.gradle.kts | mitigate | google-services plugin intentionally NOT applied in this plan — no credential surface exists yet in Wave 1 |
</threat_model>

<verification>
`flutter run -d emulator-5554` launches the real Memocard app (not the counter demo, not the spike). All 5 tabs are tappable, theme is Academic Precision (Action Blue #0066cc primary), `flutter analyze` and `flutter test test/core/` are clean.
</verification>

<success_criteria>
- Repo root has a real, buildable Flutter project (`pubspec.yaml`, `lib/`, `android/`) with `spike_platform/` removed
- `flutter run -d emulator-5554` shows a 5-tab Memocard shell with Academic Precision theme, independently of any later-wave work
- `use_build_context_synchronously` lint is active and one placeholder page demonstrates the safe pattern
- Route/theme contracts (`routerProvider`, `AppTheme`, route path constants) are locked for Waves 2-4 and Phases 2-6 to build against
</success_criteria>

<output>
Create `.planning/phases/01-shared-foundation/01-01-SUMMARY.md` when done
</output>
