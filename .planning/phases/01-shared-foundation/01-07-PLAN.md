---
phase: 01-shared-foundation
plan: 07
type: execute
wave: 4
depends_on: ["01-01", "01-02", "01-03", "01-05", "01-06"]
files_modified:
  - ENVIRONMENT.md
  - GIT_WORKFLOW.md
  - CONTRIBUTING.md
  - DEVELOPER_GUIDE.md
autonomous: true
requirements: [FND-12]

must_haves:
  truths:
    - "A developer who has never touched this repo can read ENVIRONMENT.md and get flutter doctor + the emulator suite running without asking anyone a question"
    - "A developer adding a new feature module can read DEVELOPER_GUIDE.md and know exactly where domain/data/presentation code goes, and how to inject the 7 core providers and extend BaseSyncRepository"
    - "The no-cross-module-import rule and its self-check grep command are documented in a file every developer will actually read before their first PR"
  artifacts:
    - path: "ENVIRONMENT.md"
      provides: "Verified toolchain versions (Flutter 3.41.9, Dart 3.11.5, JDK 21 via Android Studio JBR) + emulator startup commands"
      contains: "firebase emulators:start"
    - path: "DEVELOPER_GUIDE.md"
      provides: "How to add a feature module, inject core providers, extend BaseSyncRepository, the mounted-guard pattern"
      contains: "BaseSyncRepository"
    - path: "CONTRIBUTING.md"
      provides: "Dependency-addition protocol, cross-import self-check, generated-file policy"
      contains: "grep -r \"import.*package:memocard/features/\" lib/features/"
    - path: "GIT_WORKFLOW.md"
      provides: "Branch naming and commit message conventions"
      contains: "feature/<module>-"
---

<objective>
Write the four team documentation files FND-12 requires, reflecting the ACTUAL patterns Phase 1 built (not aspirational ones) — the 7 real provider names, the real `BaseSyncRepository<TModel>` contract, the real emulator-only dev workflow, the real `mounted`-guard pattern already demonstrated in code. This is why this plan depends on every other automated Phase 1 plan (01-01, 01-02, 01-03, 01-05, 01-06): it documents what was actually built, not a plan.

Purpose: the 4 remaining developers (Phases 2-6) read these before their first line of feature code — inaccurate docs here cost 4x downstream confusion.
Output: `ENVIRONMENT.md`, `GIT_WORKFLOW.md`, `CONTRIBUTING.md`, `DEVELOPER_GUIDE.md` at repo root.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/TEAM-ASSIGNMENT.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/00-platform-spike/00-SPIKE-FINDINGS.md
@.planning/phases/01-shared-foundation/SKELETON.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: ENVIRONMENT.md and GIT_WORKFLOW.md</name>
  <files>ENVIRONMENT.md, GIT_WORKFLOW.md</files>
  <read_first>.planning/phases/00-platform-spike/00-SPIKE-FINDINGS.md, .planning/TEAM-ASSIGNMENT.md (§ "Quy ước branch và commit")</read_first>
  <action>
Write `ENVIRONMENT.md`: required toolchain with VERIFIED versions from Phase 0 (Flutter 3.41.9 bundling Dart 3.11.5 — not a floor, an exact known-working pin; Android Studio 2024.1+ with an AVD using a Google APIs system image, NOT plain AOSP — Firebase Auth requires Google Play Services; JDK 21 already bundled with Android Studio at `Android Studio\jbr`, needed by `firebase-tools`' Firestore emulator; `firebase-tools` + `flutterfire_cli` via `npm install -g firebase-tools` / `dart pub global activate flutterfire_cli`). Document the exact emulator startup command this phase's plans depend on: `firebase emulators:start --only firestore,auth --project demo-memocard-dev` (ports: Auth 9099, Firestore 8080, UI 4000), and note the Android emulator reaches the host via `10.0.2.2`, not `localhost`. Include the `flutter run -d emulator-5554` command and a note that `flutter devices` is how to find the correct device id if it differs (Phase 0 found `-d android` does not work — must use the exact emulator id).

Write `GIT_WORKFLOW.md`: branch naming `feature/<module>-<short-name>` / `fix/<module>-<short-name>` (module = one of `auth`, `flashcard_set`, `learning`, `classroom`, `quiz`, or `core` for Phase-1-only work), commit message convention (specific, not "update"/"fix bug" — git history is graded individual-contribution evidence per TEAM-ASSIGNMENT.md), and the rule that `lib/core/` is locked after Phase 1 — any change needs a team-visible PR, never bundled into a feature PR.
  </action>
  <verify>
    <automated>cd "C:\Users\LENOVO\AndroidStudioProjects\prm_proejct" && test -f ENVIRONMENT.md && test -f GIT_WORKFLOW.md && grep -q "firebase emulators:start" ENVIRONMENT.md && grep -q "feature/<module>-" GIT_WORKFLOW.md</automated>
  </verify>
  <acceptance_criteria>
    - `ENVIRONMENT.md` contains the exact command `firebase emulators:start --only firestore,auth --project demo-memocard-dev`
    - `ENVIRONMENT.md` states Flutter 3.41.9 / Dart 3.11.5 as verified (not aspirational) versions
    - `GIT_WORKFLOW.md` documents the `feature/<module>-<short-name>` branch pattern and the `lib/core/` lock rule
  </acceptance_criteria>
  <done>ENVIRONMENT.md and GIT_WORKFLOW.md exist with accurate, Phase-0/Phase-1-verified commands and conventions.</done>
</task>

<task type="auto">
  <name>Task 2: CONTRIBUTING.md and DEVELOPER_GUIDE.md</name>
  <files>CONTRIBUTING.md, DEVELOPER_GUIDE.md</files>
  <read_first>lib/core/repository/base_sync_repository.dart, lib/core/providers/auth_state_provider.dart, lib/features/auth/presentation/pages/profile_placeholder_page.dart, .planning/research/PITFALLS.md (Pitfall 8 pubspec merge hell, Pitfall 9 generated files)</read_first>
  <action>
Write `CONTRIBUTING.md`: the dependency-addition protocol (one person per day adds packages to `pubspec.yaml`, per TEAM-ASSIGNMENT.md Golden Rule 3), the exact self-check command before every push: `grep -r "import.*package:memocard/features/" lib/features/` (any match outside the importing feature's own folder = violation, must go through a `core/` provider instead), the generated-files policy (`*.g.dart`/`*.freezed.dart` are gitignored — regenerate after every pull with `dart run build_runner build --delete-conflicting-outputs`, never commit them).

Write `DEVELOPER_GUIDE.md`: (1) folder structure for a new feature module (`lib/features/<module>/{domain/{entities,repositories,usecases},data/{datasources,repositories,models},presentation/{providers,pages,widgets}}` per ARCHITECTURE.md's tree); (2) how to inject each of the 7 core providers by name (`authStateProvider`, `currentUserProvider`, `userRoleProvider`, `connectivityProvider`, `databaseProvider`, `firestoreProvider`, `sharedPrefsProvider`) with one short `ref.watch(...)` example per provider; (3) how to extend `BaseSyncRepository<TModel>` for a new feature repository — list the 7 hooks a subclass must implement (`getCached`, `getRemote`, `upsertCache`, `markSynced`, `syncedAtOf`, `writeRemote`, `writeCache`) plus the required `idOf` hook; (4) the canonical `mounted`-guard pattern, pointing at `lib/features/auth/presentation/pages/profile_placeholder_page.dart` as the reference implementation, with the rule stated plainly: never call `setState()` after an `await` without `if (!mounted) return;` immediately before it — this is what `use_build_context_synchronously` in `analysis_options.yaml` enforces; (5) a note that `main.dart` currently uses dummy/emulator-only Firebase options (`lib/core/firebase/firebase_dev_options.dart`) and will switch to the real `DefaultFirebaseOptions.currentPlatform` once 01-04-PLAN.md's real-project checkpoint completes — daily dev should keep using the emulator regardless (TEAM-ASSIGNMENT.md Golden Rule 5).
  </action>
  <verify>
    <automated>cd "C:\Users\LENOVO\AndroidStudioProjects\prm_proejct" && test -f CONTRIBUTING.md && test -f DEVELOPER_GUIDE.md && grep -q "package:memocard/features/" CONTRIBUTING.md && grep -q "BaseSyncRepository" DEVELOPER_GUIDE.md</automated>
  </verify>
  <acceptance_criteria>
    - `CONTRIBUTING.md` contains the exact grep self-check command for cross-feature imports
    - `DEVELOPER_GUIDE.md` names all 7 core providers by their exact identifier and lists all 7 `BaseSyncRepository` hooks a subclass must implement
    - `DEVELOPER_GUIDE.md` references `profile_placeholder_page.dart` as the mounted-guard reference implementation
  </acceptance_criteria>
  <done>CONTRIBUTING.md and DEVELOPER_GUIDE.md accurately document Phase 1's real, as-built patterns for the 4 remaining developers to onboard against.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Documentation-only plan, no code execution surface |
</threat_model>

<verification>
All 4 files exist at repo root with content matching the acceptance criteria above.
</verification>

<success_criteria>
The 4 required team documents exist, are accurate to what Phase 1 actually built, and give the other 4 developers everything they need to start Phase 2-6 without re-deriving Phase 1's conventions — FND-12 satisfied.
</success_criteria>

<output>
Create `.planning/phases/01-shared-foundation/01-07-SUMMARY.md` when done
</output>
