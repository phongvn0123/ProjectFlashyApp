---
phase: 01-shared-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - lib/core/database/sqflite/schema.dart
  - lib/core/database/sqflite/database_service.dart
  - integration_test/database/sqflite_schema_test.dart
autonomous: true
requirements: [FND-05]

must_haves:
  truths:
    - "The local SQLite database, once opened on Android, contains all 18 ERD tables"
    - "Every one of the 18 tables has server_id, dirty_at, and synced_at columns for sync bookkeeping"
    - "Opening the database is idempotent — reopening an existing DB file does not error or duplicate tables"
  artifacts:
    - path: "lib/core/database/sqflite/schema.dart"
      provides: "18 CREATE TABLE SQL string constants, one per ERD table"
      contains: "CREATE TABLE"
    - path: "lib/core/database/sqflite/database_service.dart"
      provides: "DatabaseService.open() — singleton-style Database opener running the 18 CREATE TABLE statements in FK-safe order"
      exports: ["DatabaseService"]
  key_links:
    - from: "lib/core/database/sqflite/database_service.dart"
      to: "lib/core/database/sqflite/schema.dart"
      via: "onCreate callback executes every CREATE TABLE constant"
      pattern: "await db.execute\\(create"
---

<objective>
Define the full 18-table SQLite schema (per the ERD, with `server_id`/`dirty_at`/`synced_at` sync metadata on every table) and a `DatabaseService` that opens/creates it on Android. This is the local-cache half of Phase 1's dual-source foundation — the Firestore half is 01-03-PLAN.md, running in parallel (no file overlap).

Purpose: give the 7-core-providers plan (01-05) and the base-repository plan (01-06) a concrete, tested `Database` to build against, and give Phases 2-6 the exact table/column names they'll read/write.
Output: `lib/core/database/sqflite/schema.dart`, `lib/core/database/sqflite/database_service.dart`, an `integration_test` proving all 18 tables + sync columns exist on a real Android emulator.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-shared-foundation/01-RESEARCH.md
@.planning/phases/01-shared-foundation/01-VALIDATION.md
</context>

<interfaces>
<!-- 01-RESEARCH.md § "SQLite Schema: 18 Tables with Sync Metadata" (~line 189-466) contains the full CREATE TABLE SQL for all 18 tables. Use it verbatim EXCEPT for one fix: -->

**Fix required:** the `session_cards` table in 01-RESEARCH.md's SQL is missing a `server_id TEXT UNIQUE` column (it only has `dirty_at`/`synced_at`). ROADMAP.md Phase 1 success criterion #1 explicitly requires `server_id`, `dirty_at`, AND `synced_at` "on every table" with no exception — add `server_id TEXT UNIQUE` to `session_cards` to satisfy this. All other 17 tables in 01-RESEARCH.md's SQL already have all three sync columns; verify this while transcribing (do not introduce the same omission elsewhere).

The 18 tables, in FK-safe creation order (parents before children — this exact order avoids `FOREIGN KEY` constraint errors on `onCreate`): `users`, `flashcard_sets`, `flashcards`, `card_progress`, `learning_sessions`, `session_cards`, `favorite_sets`, `classrooms`, `class_members`, `assigned_sets`, `assignment_progress`, `class_activities`, `quizzes`, `quiz_questions`, `quiz_options`, `quiz_attempts`, `quiz_answers`, `quiz_assignments`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write the 18-table schema.dart</name>
  <files>lib/core/database/sqflite/schema.dart</files>
  <read_first>.planning/phases/01-shared-foundation/01-RESEARCH.md (§ 2 SQLite Schema, ~line 189-466)</read_first>
  <behavior>
    - `kAllTableCreateStatements` (a `List<String>` of all 18 `CREATE TABLE` statements, in the FK-safe order given in `<interfaces>`) has exactly 18 entries
    - Every entry in `kAllTableCreateStatements` contains the substrings `server_id`, `dirty_at`, and `synced_at`
    - Each `CREATE TABLE` statement's table name (extracted via a simple regex/parse in the test) matches one of the 18 expected names, with no duplicates and no omissions
  </behavior>
  <action>
Create `lib/core/database/sqflite/schema.dart` with one named `String` constant per table (`createUsersTable`, `createFlashcardSetsTable`, `createFlashcardsTable`, `createCardProgressTable`, `createLearningSessionsTable`, `createSessionCardsTable`, `createFavoriteSetsTable`, `createClassroomsTable`, `createClassMembersTable`, `createAssignedSetsTable`, `createAssignmentProgressTable`, `createClassActivitiesTable`, `createQuizzesTable`, `createQuizQuestionsTable`, `createQuizOptionsTable`, `createQuizAttemptsTable`, `createQuizAnswersTable`, `createQuizAssignmentsTable`), transcribed from 01-RESEARCH.md's SQL, applying the `session_cards` `server_id` fix from `<interfaces>`. Also export `final List<String> kAllTableCreateStatements = [createUsersTable, ..., createQuizAssignmentsTable];` in the exact FK-safe order listed in `<interfaces>` — this list is what `database_service.dart` (Task 2) iterates over in `onCreate`.

Write `test/core/database/schema_test.dart` (plain `flutter_test`, no platform channel needed — this is pure string inspection, not a real DB open) covering the `<behavior>` block: assert `kAllTableCreateStatements.length == 18`; assert every entry contains `'server_id'`, `'dirty_at'`, `'synced_at'`; assert the 18 extracted table names (via a regex like `RegExp(r'CREATE TABLE IF NOT EXISTS (\w+)')`) exactly match the expected 18-name set with no duplicates.
  </action>
  <verify>
    <automated>cd "C:\Users\LENOVO\AndroidStudioProjects\prm_proejct" && flutter test test/core/database/schema_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/core/database/schema_test.dart` passes with 0 failures
    - `grep -c "CREATE TABLE" lib/core/database/sqflite/schema.dart` returns `18`
    - `grep -A2 "createSessionCardsTable" lib/core/database/sqflite/schema.dart | grep -c "server_id"` returns a count >= 1
  </acceptance_criteria>
  <done>schema.dart defines all 18 tables with server_id/dirty_at/synced_at on every one, proven by a pure-Dart unit test (no emulator needed for this task).</done>
</task>

<task type="auto">
  <name>Task 2: DatabaseService — open/create on Android, verify with a real integration test</name>
  <files>lib/core/database/sqflite/database_service.dart, integration_test/database/sqflite_schema_test.dart</files>
  <read_first>lib/core/database/sqflite/schema.dart (from Task 1), .planning/phases/01-shared-foundation/01-VALIDATION.md (§ Test Infrastructure — sqflite requires a platform channel, cannot run under bare `flutter test`)</read_first>
  <action>
Create `lib/core/database/sqflite/database_service.dart` with a `DatabaseService` class exposing `static Future&lt;Database&gt; open()`: resolves `getDatabasesPath()`, opens `memocard.db` at `version: 1`, `onCreate: (db, version) async { for (final stmt in kAllTableCreateStatements) { await db.execute(stmt); } }`. Add a code comment documenting the migration strategy per 01-RESEARCH.md: future schema changes bump `version` and add an `onUpgrade` callback; squash migrations weekly to keep setup time under 500ms (no `onUpgrade` implementation needed yet — version 1 is the baseline, nothing to migrate from).

Create `integration_test/database/sqflite_schema_test.dart`: calls `DatabaseService.open()` on the real Android emulator, then queries `SELECT name FROM sqlite_master WHERE type='table'` and asserts all 18 expected table names are present (ignoring sqflite's own internal `android_metadata`/`sqlite_sequence` tables if present); for at least 3 representative tables (`users`, `flashcard_sets`, `quiz_attempts`), runs `PRAGMA table_info(<table>)` and asserts the column list contains `server_id`, `dirty_at`, `synced_at`. Also re-open the database a second time in the same test (simulating app restart) and assert no exception is thrown and the table count is unchanged (idempotency — `CREATE TABLE IF NOT EXISTS` guards this, but the test proves it end-to-end on the real platform channel).
  </action>
  <verify>
    <automated>cd "C:\Users\LENOVO\AndroidStudioProjects\prm_proejct" && flutter test integration_test/database/sqflite_schema_test.dart -d emulator-5554</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test integration_test/database/sqflite_schema_test.dart -d emulator-5554` passes with 0 failures on a running Android emulator
    - The test asserts exactly 18 application tables exist (not more, not fewer)
    - The test asserts `server_id`/`dirty_at`/`synced_at` columns exist on at least 3 tables via `PRAGMA table_info`
    - The test proves reopening the DB is idempotent (no error, same table count)
  </acceptance_criteria>
  <done>DatabaseService.open() creates all 18 tables with sync metadata on a real Android device, proven by integration test; reopening is safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| App ↔ local SQLite file | Device-local, app-private storage (`getDatabasesPath()`), not exposed to other apps unless device is rooted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-01-02 | Tampering | `database_service.dart` `onCreate` | mitigate | All 18 `CREATE TABLE` statements use parameterless static SQL (no string interpolation of external input) — no SQL injection surface exists at schema-creation time |
| T-01-03 | Information Disclosure | SQLite file at rest | accept | Per 01-RESEARCH.md Security Domain: SQLite stores non-sensitive cache data only; sensitive tokens live in Firebase Auth's native keystore, never in this schema |
</threat_model>

<verification>
`flutter test test/core/database/schema_test.dart` (pure Dart, fast) and `flutter test integration_test/database/sqflite_schema_test.dart -d emulator-5554` (real device) both pass.
</verification>

<success_criteria>
All 18 ERD tables exist with server_id/dirty_at/synced_at, openable and idempotent on a real Android emulator — FND-05 fully satisfied.
</success_criteria>

<output>
Create `.planning/phases/01-shared-foundation/01-02-SUMMARY.md` when done
</output>
