---
phase: 01-shared-foundation
plan: 08
type: execute
wave: 4
depends_on: ["01-01", "01-05", "01-06"]
files_modified: []
autonomous: false
requirements: [FND-08, FND-09, FND-11, FND-12]

must_haves:
  truths:
    - "The user has personally run the app on an emulator and confirmed the 5-tab shell, theme, and auth redirect work as described in every earlier plan's automated tests"
    - "The user has confirmed the documented toolchain versions in ENVIRONMENT.md match their actual machine"
---

<objective>
Close out the Phase 1 walking skeleton with a single human-verify checkpoint: run the real app end-to-end and confirm it visually matches the Academic Precision design system, and sanity-check the team docs against the user's real machine. This is the one Manual-Only Verification 01-VALIDATION.md calls out for FND-08/FND-09 (visual fidelity to Stitch mockups can't be asserted by a widget test) and a lightweight version of the FND-11/FND-12 "clone fresh, follow docs" check (full multi-developer clone verification naturally happens as the other 4 people start Phase 2-6).

Purpose: catch anything the automated tests structurally cannot (does it *look* like Academic Precision, do the documented commands actually match this machine) before calling Phase 1 done.
Output: none (verification-only plan, no files modified).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/reference/ui-screens/academic_precision/DESIGN.md
@.planning/phases/01-shared-foundation/01-VALIDATION.md
@ENVIRONMENT.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Confirm the walking skeleton runs and matches the design system</name>
  <action>All automated work for Phase 1's walking skeleton (5-tab shell, Academic Precision theme, auth redirect, local Firebase Emulator wiring) is already built and unit/integration tested by 01-01, 01-05, and 01-06. This task is the human visual/environment confirmation pass — nothing left to automate.</action>
  <what-built>
A real 5-tab Memocard app (Trang chủ / Thư viện / Lớp học / Bài kiểm tra / Cá nhân) with the Academic Precision light/dark theme, an auth redirect to `/auth/login` when signed out, all running against the local Firebase Emulator Suite — plus 4 team documentation files at the repo root.
  </what-built>
  <how-to-verify>
    1. In one terminal: `firebase emulators:start --only firestore,auth --project demo-memocard-dev` (wait for "All emulators ready!").
    2. In another terminal: `flutter run -d emulator-5554`.
    3. Confirm the app launches directly to a login placeholder screen (you are not signed in) — this proves the auth redirect from 01-05 works.
    4. There is no way to sign in yet (Phase 2 builds that) — confirm being stuck on the login placeholder is the EXPECTED behavior per FND-08's "redirects unauthenticated users to login automatically" criterion, not a bug.
    5. Toggle your emulator's system theme between light and dark (Settings → Display → Dark theme) and relaunch — confirm the app follows: light shows a near-white background (`#f9f9ff`) with Action Blue (`#0066cc`) buttons/accents; dark shows a deep charcoal background (`#2e3037`) with a lighter blue accent. Compare against `.planning/reference/ui-screens/academic_precision/DESIGN.md`.
    6. Open `ENVIRONMENT.md` and confirm the listed Flutter/Dart/JDK/Node versions match what `flutter --version`, `dart --version`, and `java -version` report on your machine.
  </how-to-verify>
  <resume-signal>Type "approved" if steps 3-6 all match, or describe exactly what differs (screenshot welcome) and Claude will open a gap-closure plan.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| N/A | Human verification only, no code changes in this plan |
</threat_model>

<verification>
User confirms "approved" per the resume-signal above.
</verification>

<success_criteria>
The walking skeleton is confirmed working end-to-end by a human on a real emulator, and the team docs are confirmed accurate to the user's real machine — Phase 1 is ready to hand off to Phases 2-6.
</success_criteria>

<output>
Create `.planning/phases/01-shared-foundation/01-08-SUMMARY.md` when done
</output>
