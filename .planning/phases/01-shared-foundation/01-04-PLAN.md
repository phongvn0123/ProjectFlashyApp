---
phase: 01-shared-foundation
plan: 04
type: execute
wave: 3
depends_on: ["01-03"]
files_modified:
  - lib/firebase_options.dart
  - android/app/google-services.json
  - android/app/build.gradle.kts
  - android/build.gradle.kts
autonomous: false
requirements: [FND-06]
user_setup:
  - service: firebase
    why: "The team's real shared Firebase Spark-tier project must be created and authenticated via Google OAuth in a browser — Claude cannot complete this login flow via CLI."
    env_vars: []
    dashboard_config:
      - task: "firebase login (browser OAuth)"
        location: "CLI, opens https://accounts.google.com"
      - task: "Create or select the shared 'memocard' Firebase project"
        location: "https://console.firebase.google.com"
      - task: "flutterfire configure --project=<real-project-id> --platforms=android (registers com.memocard.memocard, generates lib/firebase_options.dart + android/app/google-services.json)"
        location: "CLI"
      - task: "firebase deploy --only firestore:rules,firestore:indexes --project=<real-project-id>"
        location: "CLI, pushes 01-03-PLAN.md's firestore.rules/firestore.indexes.json live"

must_haves:
  truths:
    - "The team's real Firebase project exists and is shared by all 5 developers, not just the person who ran this checkpoint"
    - "firestore.rules and firestore.indexes.json from 01-03-PLAN.md are actually deployed to that real project (not just present locally)"
    - "android/app/google-services.json exists locally and is gitignored (never committed)"
  artifacts:
    - path: "lib/firebase_options.dart"
      provides: "DefaultFirebaseOptions.currentPlatform generated by flutterfire configure for the real project"
      exports: ["DefaultFirebaseOptions"]
  key_links:
    - from: "android/app/build.gradle.kts"
      to: "android/app/google-services.json"
      via: "com.google.gms.google-services Gradle plugin, auto-added by flutterfire configure"
      pattern: "com.google.gms.google-services"
---

<objective>
Create/select the team's real shared Firebase project and deploy 01-03-PLAN.md's Firestore rules and indexes to it. This is the ONE piece of Phase 1 that genuinely cannot be automated — `firebase login` requires a browser-based Google OAuth flow — so it is isolated in its own checkpoint-only plan per the "checkpoint + implementation in same plan" split rule.

**This plan does NOT block any other Phase 1 work.** Every other plan in this phase (including 01-05's Firebase-consuming core providers) targets the local Emulator Suite (`demo-memocard-dev`, no real project needed), matching TEAM-ASSIGNMENT.md Golden Rule 5 ("dev via emulator, never the real project"). This checkpoint only needs to complete before Phase 2 wants to test against real (non-emulated) Firebase Auth/Firestore, or before Phase 7's final integration/demo.

Purpose: unblock the eventual real-backend milestone without gating today's foundation work on it.
Output: `lib/firebase_options.dart`, `android/app/google-services.json` (gitignored), Gradle files updated by `flutterfire configure`, real project's Firestore rules/indexes deployed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/TEAM-ASSIGNMENT.md
@.planning/phases/01-shared-foundation/01-03-PLAN.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Create the real Firebase project and deploy rules</name>
  <action>Authenticate to Firebase (browser OAuth), create/select the real shared project, run flutterfire configure, and deploy firestore.rules/firestore.indexes.json to it — Claude cannot perform the OAuth login step, everything else can be run by Claude on request once step 2 below is done.</action>
  <instructions>
    1. If not already installed: `npm install -g firebase-tools` and `dart pub global activate flutterfire_cli`. Verify with `firebase --version` and `flutterfire --version`.
    2. Run `firebase login` — this opens a browser. Sign in with the Google account the team has agreed will own the shared Spark-tier Firebase project (per CLAUDE.md: "5 người dùng chung 1 Firebase project").
    3. In the [Firebase Console](https://console.firebase.google.com), create a new project (or select the existing one if the team already created it) — suggested name/ID: `memocard-prm393-gr6`. Confirm the plan is Spark (free tier).
    4. From the repo root, run: `flutterfire configure --project=<the-real-project-id> --platforms=android`. When prompted for the Android package name, use `com.memocard.memocard` (must match `android/app/build.gradle.kts`'s `applicationId`, set in 01-01-PLAN.md Task 1). This generates `lib/firebase_options.dart` and downloads `android/app/google-services.json`, and auto-edits `android/build.gradle.kts`/`android/app/build.gradle.kts` to add the `com.google.gms.google-services` Gradle plugin.
    5. Run `firebase deploy --only firestore:rules,firestore:indexes --project=<the-real-project-id>` to push 01-03-PLAN.md's `firestore.rules`/`firestore.indexes.json` to the real project.
    6. Confirm `android/app/google-services.json` is covered by `.gitignore` (01-01-PLAN.md Task 1 already added `**/google-services.json` — verify with `git check-ignore android/app/google-services.json`, expect exit code 0).
  </instructions>
  <verification>Claude checks: `lib/firebase_options.dart` exists and its `projectId` field matches the project ID you provide; `git check-ignore android/app/google-services.json` exits 0; `firebase deploy` output (paste it or re-run it) confirms rules/indexes were released without errors.</verification>
  <resume-signal>Reply with the real Firebase project ID once steps 1-6 are complete. Claude will verify lib/firebase_options.dart exists and references that project ID, and that google-services.json is gitignored, before marking this plan done.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Human ↔ Google OAuth | Outside Claude's execution boundary entirely — no code runs here |
| Real Firebase project ↔ 5 shared developers | All 5 developers will use the same credentials; a leaked `google-services.json` exposes the shared project's API key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-01-07 | Information Disclosure | `android/app/google-services.json` | mitigate | Gitignored per 01-01-PLAN.md; this checkpoint's step 6 re-verifies it before marking done |
| T-01-08 | Elevation of Privilege | Real project's Firestore rules | mitigate | Step 5 deploys the exact rules proven safe by 01-03-PLAN.md's integration test — no ad-hoc rule authored during this checkpoint |
</threat_model>

<verification>
`git check-ignore android/app/google-services.json` exits 0; `lib/firebase_options.dart` exists and contains the confirmed real project ID; `firebase deploy --only firestore:rules,firestore:indexes` output confirms a successful deploy.
</verification>

<success_criteria>
Real shared Firebase project exists, real Firestore rules/indexes are deployed, real Android app credentials are present locally and gitignored — the FND-06 "deployed" success criterion is satisfied without having blocked any other Phase 1 work.
</success_criteria>

<output>
Create `.planning/phases/01-shared-foundation/01-04-SUMMARY.md` when done
</output>
