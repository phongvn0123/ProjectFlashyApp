---
phase: 01-shared-foundation
plan: 05
type: execute
wave: 3
depends_on: ["01-01", "01-02", "01-03"]
files_modified:
  - lib/core/firebase/firebase_dev_options.dart
  - lib/core/providers/auth_state_provider.dart
  - lib/core/providers/current_user_provider.dart
  - lib/core/providers/user_role_provider.dart
  - lib/core/providers/connectivity_provider.dart
  - lib/core/providers/database_provider.dart
  - lib/core/providers/firestore_provider.dart
  - lib/core/providers/shared_prefs_provider.dart
  - lib/core/providers/theme_mode_provider.dart
  - lib/main.dart
  - lib/core/router/router.dart
  - test/helpers/provider_harness.dart
  - test/core/providers/providers_smoke_test.dart
  - integration_test/app_test.dart
autonomous: true
requirements: [FND-07, FND-08]

must_haves:
  truths:
    - "A feature screen can inject any of the 7 core providers via ref.watch() without redefining them"
    - "A feature screen can also read the current theme mode via ref.watch(themeModeProvider), satisfying ROADMAP's success criterion #3 (\"read current user, role, connectivity, and theme from ... core providers\") — themeModeProvider is a bonus 8th provider, not counted toward FND-07's literal '7 core providers'"
    - "No circular dependency exists in the provider graph (authState -> currentUser -> userRole is a strict forward chain)"
    - "An unauthenticated app launch redirects to /auth/login; the redirect re-evaluates when auth state changes, without discarding the GoRouter instance"
    - "The app runs entirely against the local Firebase Emulator Suite — no real Firebase project credentials required"
  artifacts:
    - path: "lib/core/providers/auth_state_provider.dart"
      provides: "authStateProvider (StreamProvider<User?>) streaming FirebaseAuth.instance.authStateChanges()"
      exports: ["authStateProvider"]
    - path: "lib/core/providers/database_provider.dart"
      provides: "databaseProvider (FutureProvider<Database>) wrapping DatabaseService.open()"
      exports: ["databaseProvider"]
    - path: "lib/core/providers/connectivity_provider.dart"
      provides: "connectivityProvider (StreamProvider<bool>) — dart:io polling, no connectivity_plus package"
      exports: ["connectivityProvider"]
    - path: "lib/core/providers/theme_mode_provider.dart"
      provides: "themeModeProvider (StateProvider<ThemeMode>, default ThemeMode.system) — bonus provider, not one of the 7"
      exports: ["themeModeProvider"]
  key_links:
    - from: "lib/core/providers/current_user_provider.dart"
      to: "lib/core/providers/auth_state_provider.dart"
      via: "ref.watch(authStateProvider)"
      pattern: "ref.watch\\(authStateProvider\\)"
    - from: "lib/core/router/router.dart"
      to: "lib/core/providers/auth_state_provider.dart"
      via: "GoRouterRefreshStream(ref.watch(authStateProvider.stream)) feeding GoRouter.refreshListenable"
      pattern: "refreshListenable"
    - from: "lib/main.dart"
      to: "lib/core/providers/shared_prefs_provider.dart"
      via: "ProviderScope override with the awaited SharedPreferences instance"
      pattern: "sharedPrefsProvider.overrideWithValue"
    - from: "lib/main.dart"
      to: "lib/core/providers/theme_mode_provider.dart"
      via: "MemocardApp watches themeModeProvider for MaterialApp.router's themeMode"
      pattern: "ref.watch(themeModeProvider)"
---

<objective>
Build the 7 shared Riverpod core providers (auth state, current user, user role, connectivity, database, Firestore, shared preferences) plus one bonus `themeModeProvider`, and wire Firebase initialization into `main.dart` — using dummy/emulator-only Firebase options so this plan does NOT depend on 01-04's real-project checkpoint. Complete FND-08 by wiring the real auth-redirect logic into the router shell built in 01-01.

Purpose: this is the contract every feature screen in Phases 2-6 injects instead of reimplementing auth/DB/connectivity/prefs/theme access — get it right once, here.
Output: `lib/core/providers/*.dart` (8 files: 7 counted toward FND-07 + 1 bonus), Firebase-initialized `main.dart`, `router.dart` with a real (not stubbed) auth redirect.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-shared-foundation/01-RESEARCH.md
@.planning/phases/00-platform-spike/00-SPIKE-FINDINGS.md
@spike_platform/lib/main.dart
</context>

<interfaces>
<!-- From 01-01-PLAN.md (already built): -->
`lib/core/router/router.dart` currently exports `routerProvider` with `redirect: (context, state) => null` and a `// TODO(01-05)` comment — this task replaces that stub.
`lib/core/router/routes.dart` exports `kHomeRoute`, `kLibraryRoute`, `kClassroomRoute`, `kQuizRoute`, `kProfileRoute`, `kLoginRoute`.
`lib/main.dart` currently applies theme via the literal constant `themeMode: ThemeMode.system` — this task replaces that literal with a provider-backed read (see `themeModeProvider` below).

<!-- From 01-02-PLAN.md (already built): -->
`lib/core/database/sqflite/database_service.dart` exports `DatabaseService.open()` (`Future<Database>`).

<!-- From 01-03-PLAN.md (already built): -->
`.firebaserc` pins project alias `demo-memocard-dev`; `firebase.json` runs Auth emulator on port 9099, Firestore on port 8080.

<!-- 01-RESEARCH.md § 4 "Riverpod 3.x Core Providers (7 Shared)" (~line 510-683) has working code for all 7 providers, plus a "Bonus: Theme Provider" example. Use it as the base, with 3 deviations documented below. -->

**Deviation 1 — connectivityProvider:** 01-RESEARCH.md's example imports `package:connectivity_plus/connectivity_plus.dart`, which is NOT in CLAUDE.md's mandatory package list and has no entry in 01-RESEARCH.md's Package Legitimacy Audit table. Do not add this package. Implement `connectivityProvider` as a `StreamProvider<bool>` using `dart:io`'s `InternetAddress.lookup('firebase.google.com')` polled every 5 seconds via `Stream.periodic(const Duration(seconds: 5)).asyncMap((_) async { try { final r = await InternetAddress.lookup('firebase.google.com'); return r.isNotEmpty && r.first.rawAddress.isNotEmpty; } catch (_) { return false; } })`, seeded with an immediate first check so the UI doesn't show "loading" for the first 5 seconds.

**Deviation 2 — main.dart uses dummy/emulator Firebase options, not a generated firebase_options.dart:** 01-04's checkpoint (real project) has not necessarily run yet, and per TEAM-ASSIGNMENT.md Golden Rule 5, daily dev should target the emulator anyway. Create `lib/core/firebase/firebase_dev_options.dart` with a manually-authored `const FirebaseOptions kDevFirebaseOptions = FirebaseOptions(apiKey: 'demo-emulator-key', appId: '1:000000000000:android:0000000000000000', messagingSenderId: '000000000000', projectId: 'demo-memocard-dev', storageBucket: 'demo-memocard-dev.appspot.com');` — matching the `demo-memocard-dev` project alias 01-03 pinned in `.firebaserc`. Document in a comment: once 01-04's `flutterfire configure` produces a real `lib/firebase_options.dart`, a future plan should switch `main.dart` to `DefaultFirebaseOptions.currentPlatform` for real-backend runs — this swap is intentionally NOT done here.

**Deviation 3 — bonus `themeModeProvider`:** ROADMAP.md's Phase 1 success criterion #3 says a feature screen must be able to "read current user, role, connectivity, and theme from the ... core providers" — but 01-RESEARCH.md's own FND-07 count is exactly 7, and its Bonus Theme Provider example uses `StateNotifierProvider` backed by `SharedPreferences`, which is Phase 2 PROF-04 territory (persistence), not Phase 1. Resolve this by adding `lib/core/providers/theme_mode_provider.dart` exporting `final themeModeProvider = StateProvider<ThemeMode>((ref) => ThemeMode.system);` — a simple in-memory (non-persisted) provider. This satisfies ROADMAP's literal wording (theme readable via a Riverpod provider) without claiming it as one of FND-07's counted 7, and without doing Phase 2's persistence work early.

`main.dart` Firebase init pattern (per 00-SPIKE-FINDINGS.md, proven twice): wrap `Firebase.initializeApp(options: kDevFirebaseOptions)` in `try { ... } on FirebaseException catch (e) { if (e.code != 'duplicate-app') rethrow; }`. Then `await FirebaseAuth.instance.useAuthEmulator('10.0.2.2', 9099);` (MUST be awaited — Phase 0 CR-01 finding) and `FirebaseFirestore.instance.useFirestoreEmulator('10.0.2.2', 8080);` (synchronous, host `10.0.2.2` is the Android emulator's route to the host machine, not `localhost`). Do NOT apply the `com.google.gms.google-services` Gradle plugin in this plan — no `google-services.json` exists yet without 01-04, and this plan must build without one.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: The 7 core providers + bonus themeModeProvider + test harness</name>
  <files>lib/core/providers/auth_state_provider.dart, lib/core/providers/current_user_provider.dart, lib/core/providers/user_role_provider.dart, lib/core/providers/connectivity_provider.dart, lib/core/providers/database_provider.dart, lib/core/providers/firestore_provider.dart, lib/core/providers/shared_prefs_provider.dart, lib/core/providers/theme_mode_provider.dart, test/helpers/provider_harness.dart, test/core/providers/providers_smoke_test.dart</files>
  <read_first>.planning/phases/01-shared-foundation/01-RESEARCH.md (§ 4 Riverpod Core Providers, ~line 510-683), lib/core/database/sqflite/database_service.dart, lib/core/constants/firestore_collections.dart</read_first>
  <behavior>
    - `ProviderContainer` with `databaseProvider`/`firestoreProvider`/`sharedPrefsProvider` all overridden with fakes builds without throwing (no circular dependency in the provider graph)
    - `authStateProvider` is a `StreamProvider<User?>`; when overridden with a fake stream emitting `null`, `currentUserProvider` resolves to `null` without error
    - `currentUserProvider` only reads `authStateProvider` (never the reverse) — `userRoleProvider` only reads `currentUserProvider` — enforced by grep in acceptance criteria, since Dart has no static cycle-detection for Riverpod graphs
    - `themeModeProvider`'s default value (no overrides) reads as `ThemeMode.system`
  </behavior>
  <action>
Create all 7 provider files per 01-RESEARCH.md § 4, applying Deviation 1 for `connectivity_provider.dart`:
- `auth_state_provider.dart`: `final authStateProvider = StreamProvider<User?>((ref) => FirebaseAuth.instance.authStateChanges());`
- `current_user_provider.dart`: `FutureProvider<Map<String, dynamic>?>` that watches `authStateProvider`, and on a non-null Firebase user, fetches `users/{uid}` from `ref.watch(firestoreProvider)` using `kUsersCollection` from `firestore_collections.dart`. Since the `UserEntity`/domain model doesn't exist yet in Phase 1 (models are a Phase 2+ concern per TEAM-ASSIGNMENT.md — Person 1 owns `User`), return the raw `Map<String, dynamic>?` from the Firestore document for now, with a comment noting Phase 2 will introduce a typed `UserEntity`/`fromFirestore` and this provider's return type will narrow then — do not invent a `UserEntity` class in this plan.
- `user_role_provider.dart`: `FutureProvider<String?>` reading `currentUserProvider`'s `role` field.
- `connectivity_provider.dart`: per Deviation 1 above, `dart:io`-based, no new package.
- `database_provider.dart`: `final databaseProvider = FutureProvider<Database>((ref) => DatabaseService.open());`
- `firestore_provider.dart`: `final firestoreProvider = Provider<FirebaseFirestore>((ref) => FirebaseFirestore.instance);` (emulator redirection already happens once in `main.dart`, not per-provider — do not call `useFirestoreEmulator` again here, it would throw on a second call).
- `shared_prefs_provider.dart`: `final sharedPrefsProvider = Provider<SharedPreferences>((ref) => throw UnimplementedError('Override in main.dart ProviderScope after awaiting SharedPreferences.getInstance()'));` — this is intentionally an override-only provider per 01-RESEARCH.md's "OR: Initialize in main.dart and inject" pattern (the async `FutureProvider` alternative is rejected here because most consumers want synchronous access to prefs).
- `theme_mode_provider.dart`: per Deviation 3, `final themeModeProvider = StateProvider<ThemeMode>((ref) => ThemeMode.system);` with a comment noting this is a bonus 8th provider (in-memory only, no persistence — Phase 2 PROF-04 adds SharedPreferences-backed persistence and a toggle action).

Create `test/helpers/provider_harness.dart`: a helper function `ProviderContainer buildTestContainer({List<Override> extraOverrides = const []})` that returns a `ProviderContainer` with `databaseProvider`, `firestoreProvider`, and `sharedPrefsProvider` all pre-overridden with in-memory fakes (a `FakeFirebaseFirestore`-style minimal stub is NOT required — overriding with `Provider.overrideWithValue` using bare mock/fake instances sufficient for a "does the graph build" smoke test is enough), plus `extraOverrides` merged in for provider-specific tests.

Create `test/core/providers/providers_smoke_test.dart` covering the `<behavior>` block above, using `provider_harness.dart`.
  </action>
  <verify>
    <automated>cd "C:\Users\LENOVO\AndroidStudioProjects\prm_proejct" && flutter test test/core/providers/providers_smoke_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/core/providers/providers_smoke_test.dart` passes with 0 failures
    - `grep -rn "connectivity_plus" pubspec.yaml lib/` returns no matches (Deviation 1 honored — no unapproved package added)
    - `grep -n "ref.watch(authStateProvider)" lib/core/providers/current_user_provider.dart` matches, and `grep -n "currentUserProvider" lib/core/providers/auth_state_provider.dart` returns no match (no reverse dependency)
    - All 8 files exist under `lib/core/providers/` (7 counted toward FND-07 + `theme_mode_provider.dart`)
  </acceptance_criteria>
  <done>All 7 core providers plus the bonus themeModeProvider exist, build without circular dependency, and are proven via a ProviderContainer smoke test with fakes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Firebase init in main.dart, provider-backed theme, and the real auth-redirect in router.dart</name>
  <files>lib/core/firebase/firebase_dev_options.dart, lib/main.dart, lib/core/router/router.dart, integration_test/app_test.dart</files>
  <read_first>lib/main.dart (from 01-01-PLAN.md), lib/core/router/router.dart (from 01-01-PLAN.md), .planning/phases/00-platform-spike/00-SPIKE-FINDINGS.md (§ Deviations from Plan — duplicate-app and useAuthEmulator await findings)</read_first>
  <action>
Create `lib/core/firebase/firebase_dev_options.dart` per Deviation 2 in `<interfaces>` above.

Rewrite `lib/main.dart`'s `main()` to `Future<void> main() async { WidgetsFlutterBinding.ensureInitialized(); try { await Firebase.initializeApp(options: kDevFirebaseOptions); } on FirebaseException catch (e) { if (e.code != 'duplicate-app') rethrow; } await FirebaseAuth.instance.useAuthEmulator('10.0.2.2', 9099); FirebaseFirestore.instance.useFirestoreEmulator('10.0.2.2', 8080); final prefs = await SharedPreferences.getInstance(); runApp(ProviderScope(overrides: [sharedPrefsProvider.overrideWithValue(prefs)], child: const MemocardApp())); }` per the exact pattern in `<interfaces>`. Update `MemocardApp.build` to replace the literal `themeMode: ThemeMode.system` from 01-01 with `themeMode: ref.watch(themeModeProvider)`.

Update `lib/core/router/router.dart`: replace the `redirect: (context, state) => null` stub. Add a small `GoRouterRefreshStream` helper class (implements `ChangeNotifier`, subscribes to a `Stream<dynamic>` in its constructor and calls `notifyListeners()` on every event, cancels the subscription in `dispose()`) — this is the standard go_router community pattern for feeding a Riverpod stream into `GoRouter.refreshListenable` without recreating the `GoRouter` instance on every auth change. Wire `routerProvider` to build the `GoRouter` ONCE with `refreshListenable: GoRouterRefreshStream(ref.watch(authStateProvider.stream))`, and implement `redirect: (context, state) { final isLoggedIn = ref.read(authStateProvider).valueOrNull != null; final isGoingToLogin = state.matchedLocation == kLoginRoute; if (!isLoggedIn && !isGoingToLogin) return kLoginRoute; if (isLoggedIn && isGoingToLogin) return kHomeRoute; return null; }` (read fresh state at redirect-time via `ref.read`, react to changes via `refreshListenable` — this is the correct combination per go_router docs, avoiding the naive "just `ref.watch` inside a `Provider<GoRouter>`" pattern which would tear down navigation state on every auth event).

Extend `integration_test/app_test.dart` (created as a placeholder in 01-01): pump the real app against the Auth Emulator with no signed-in user, assert the login placeholder page is shown (redirect works); then sign in a test user via the Auth Emulator, pump again, assert one of the 5 tab placeholder pages is shown instead.
  </action>
  <verify>
    <automated>cd "C:\Users\LENOVO\AndroidStudioProjects\prm_proejct" && firebase emulators:exec --only firestore,auth --project demo-memocard-dev "flutter test integration_test/app_test.dart -d emulator-5554" && flutter analyze</automated>
  </verify>
  <acceptance_criteria>
    - The command in `<verify>` exits 0
    - `grep -n "await FirebaseAuth.instance.useAuthEmulator" lib/main.dart` matches (await present, not fire-and-forget — Phase 0 CR-01 regression check)
    - `grep -n "on FirebaseException catch (e)" lib/main.dart` matches and the following line checks `e.code != 'duplicate-app'`
    - `grep -n "com.google.gms.google-services" android/app/build.gradle.kts` returns NO match (this plan must not have applied the plugin)
    - `grep -n "ref.watch(themeModeProvider)" lib/main.dart` matches
    - `flutter analyze` reports `No issues found!`
    - Manual: `firebase emulators:start --only firestore,auth --project demo-memocard-dev` in one terminal, then `flutter run -d emulator-5554` in another — app launches directly to `/auth/login` (no signed-in user), confirming the redirect fires
  </acceptance_criteria>
  <done>main.dart initializes Firebase against the local emulator only (no real project needed), theme is now provider-backed, auth-redirect is live in router.dart using the correct refreshListenable pattern, integration test proves both the unauthenticated-redirect and authenticated-pass-through paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| App ↔ Firebase Auth Emulator | Local-only in Phase 1 (10.0.2.2:9099); no real credentials in play |
| GoRouter redirect | Client-side only — this is UX convenience, NOT a security boundary; the real enforcement is 01-03's Firestore rules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-01-09 | Spoofing | Client-side auth redirect | accept | Redirect is UX-only; an attacker bypassing the client redirect still hits Firestore's server-enforced rules (01-03), so client-side-only enforcement here is an acceptable risk |
| T-01-10 | Tampering | `firebase_dev_options.dart` dummy API key | accept | Value is a placeholder string (`demo-emulator-key`), only valid against the local Emulator Suite — has no power against any real backend, safe to commit |
</threat_model>

<verification>
`firebase emulators:exec --only firestore,auth --project demo-memocard-dev "flutter test integration_test/app_test.dart -d emulator-5554"` passes; `flutter analyze` is clean.
</verification>

<success_criteria>
7 core providers plus the bonus themeModeProvider exist and are injectable without redefinition (FND-07, plus ROADMAP success criterion #3's "theme" wording); unauthenticated users are redirected to login and authenticated users pass through (FND-08 fully complete); app runs entirely against the local emulator with zero real Firebase dependency.
</success_criteria>

<output>
Create `.planning/phases/01-shared-foundation/01-05-SUMMARY.md` when done
</output>
