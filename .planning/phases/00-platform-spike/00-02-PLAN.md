---
phase: 00-platform-spike
plan: 02
type: execute
wave: 2
depends_on: ["00-01"]
files_modified:
  - spike_platform/lib/sqlite_service.dart
  - spike_platform/lib/firebase_service.dart
  - spike_platform/lib/main.dart
autonomous: true
requirements: [FND-01, FND-03, FND-04]

must_haves:
  truths:
    - "SQLite opens a database in Android app-private storage, inserts a row, and reads it back, returning a string beginning with 'SQLITE PASS: '"
    - "Firebase initializes, a test user signs up (or signs back in on re-run) via the local Auth Emulator, and a Firestore document round-trips (write then read), returning a string beginning with 'FIREBASE PASS: '"
    - "Both results are logged to console with an exact '[SPIKE] ' prefix and rendered in the app UI on launch"
    - "Neither service file imports sqflite_common_ffi or overrides databaseFactory"
  artifacts:
    - path: "spike_platform/lib/sqlite_service.dart"
      provides: "testSqliteInsertAndRead() — plain sqflite round-trip on Android"
      exports: ["testSqliteInsertAndRead"]
    - path: "spike_platform/lib/firebase_service.dart"
      provides: "testFirebaseInitAuthFirestore() — Auth signup/signin + Firestore write/read against emulator"
      exports: ["testFirebaseInitAuthFirestore"]
    - path: "spike_platform/lib/main.dart"
      provides: "App entry point: Firebase init + emulator targeting, auto-runs both tests, renders results, re-run buttons"
      contains: "WidgetsFlutterBinding.ensureInitialized()"
  key_links:
    - from: "spike_platform/lib/main.dart"
      to: "spike_platform/lib/sqlite_service.dart"
      via: "await testSqliteInsertAndRead() called from _runAllTests() and the 'Re-run SQLite Test' button"
      pattern: "testSqliteInsertAndRead\\("
    - from: "spike_platform/lib/main.dart"
      to: "spike_platform/lib/firebase_service.dart"
      via: "await testFirebaseInitAuthFirestore() called from _runAllTests() and the 'Re-run Firebase Test' button"
      pattern: "testFirebaseInitAuthFirestore\\("
    - from: "spike_platform/lib/main.dart"
      to: "spike_platform/lib/firebase_options_spike.dart"
      via: "Firebase.initializeApp(options: spikeFirebaseOptions) before useAuthEmulator/useFirestoreEmulator"
      pattern: "Firebase\\.initializeApp\\(options: spikeFirebaseOptions\\)"
---

<objective>
Viết logic thật của spike: `sqlite_service.dart` chứng minh `sqflite` round-trip trên Android, `firebase_service.dart` chứng minh Firebase Auth Emulator + Firestore Emulator round-trip, và `main.dart` gộp cả hai vào một màn hình duy nhất tự chạy khi khởi động, log kết quả ra console với prefix `[SPIKE] `.

Purpose: Đây là phần chứng minh nghiệp vụ cốt lõi của Phase 0 — không có phần này thì FND-01/03/04 không thể được xác nhận. Plan 00-01 đã dựng xong khung + config; plan này lấp đầy logic.
Output: Ba file Dart hoạt động được, khi `flutter run -d android` app tự động chạy cả 2 test và in `[SPIKE] SQLITE PASS/FAIL` + `[SPIKE] FIREBASE PASS/FAIL` ra console, hiển thị trên UI.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-platform-spike/00-RESEARCH.md
@.planning/phases/00-platform-spike/00-VALIDATION.md
@.planning/phases/00-platform-spike/00-01-PLAN.md
</context>

<interfaces>
<!-- Hợp đồng do Plan 00-01 tạo ra, tiêu thụ trực tiếp ở plan này. Không cần dò lại codebase. -->

Từ `spike_platform/lib/platform_config.dart` (Plan 00-01 Task 2):
```
const int kAuthEmulatorPort = 9099;
const int kFirestoreEmulatorPort = 8080;
String get kAuthEmulatorHost;   // '10.0.2.2' trên Android, 'localhost' khác
String get kFirestoreEmulatorHost;
```

Từ `spike_platform/lib/firebase_options_spike.dart` (Plan 00-01 Task 2):
```
const FirebaseOptions spikeFirebaseOptions; // apiKey KHÔNG bắt đầu bằng 'AIza', projectId 'demo-spike-project'
```

Từ 00-RESEARCH.md Pattern 3 (sqflite round-trip, không factory override) và Code Examples (main.dart mẫu đầy đủ) — dùng làm nguồn tham chiếu chính xác cho cấu trúc hàm.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Viết sqlite_service.dart (round-trip sqflite thuần trên Android)</name>
  <files>spike_platform/lib/sqlite_service.dart</files>
  <read_first>
    - .planning/phases/00-platform-spike/00-RESEARCH.md (mục "Pattern 3: Plain sqflite Round-Trip" và "Pitfall 6")
    - spike_platform/pubspec.yaml (xác nhận sqflite đã có trong dependencies, tạo ở Plan 00-01)
  </read_first>
  <action>
    Tạo `lib/sqlite_service.dart` với hàm `Future<String> testSqliteInsertAndRead()`. Import CHỈ `package:sqflite/sqflite.dart` — KHÔNG import `package:sqflite_common_ffi/sqflite_ffi.dart` dưới bất kỳ hình thức nào, KHÔNG gán `databaseFactory = databaseFactoryFfi` (per RESEARCH Pitfall 6, đây là quy tắc hồi quy chống lại các plan cũ đã bị xoá).
    Logic: bọc toàn bộ trong `try/catch`. Lấy đường dẫn bằng `getDatabasesPath()`, ghép `'$databasesPath/spike.db'`. Mở bằng `openDatabase(dbPath, version: 1, onCreate: (db, version) => db.execute('CREATE TABLE spike_test (id INTEGER PRIMARY KEY, value TEXT)'))`. Insert một row `{'value': 'hello-from-android'}` bằng `db.insert('spike_test', ...)`. Đọc lại toàn bộ bằng `db.query('spike_test')`. Đóng db bằng `db.close()`. Trả về chuỗi bắt đầu ĐÚNG với literal `'SQLITE PASS: '` nối với `rows.first` khi thành công.
    Trong khối `catch (e)`, trả về chuỗi bắt đầu ĐÚNG với literal `'SQLITE FAIL: '` nối với `e`.
  </action>
  <acceptance_criteria>
    - `spike_platform/lib/sqlite_service.dart` export hàm `testSqliteInsertAndRead()` kiểu trả về `Future<String>`
    - `grep -c "sqflite_common_ffi\|databaseFactoryFfi" spike_platform/lib/sqlite_service.dart` trả về `0`
    - `grep -c "'SQLITE PASS: '" spike_platform/lib/sqlite_service.dart` trả về ít nhất `1`
    - `grep -c "'SQLITE FAIL: '" spike_platform/lib/sqlite_service.dart` trả về ít nhất `1`
    - `cd spike_platform && flutter analyze lib/sqlite_service.dart` không báo lỗi
  </acceptance_criteria>
  <verify>
    <automated>cd spike_platform && flutter analyze lib/sqlite_service.dart</automated>
  </verify>
  <done>sqlite_service.dart tồn tại, không dùng FFI, trả về chuỗi PASS/FAIL đúng định dạng, flutter analyze sạch.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Viết firebase_service.dart (Auth signup/signin + Firestore round-trip trên Emulator)</name>
  <files>spike_platform/lib/firebase_service.dart</files>
  <read_first>
    - .planning/phases/00-platform-spike/00-RESEARCH.md (mục "Pattern 2" và "Code Examples")
    - .planning/phases/00-platform-spike/00-VALIDATION.md (mục "Security Domain > Applicable ASVS Categories" — V2 Authentication)
    - spike_platform/lib/platform_config.dart, spike_platform/lib/firebase_options_spike.dart (tạo ở Plan 00-01 Task 2 — chỉ đọc để biết identifier có sẵn, KHÔNG cần import trực tiếp vào file này)
  </read_first>
  <action>
    Tạo `lib/firebase_service.dart` với hàm `Future<String> testFirebaseInitAuthFirestore()`. Import `package:firebase_auth/firebase_auth.dart` và `package:cloud_firestore/cloud_firestore.dart`. Hàm này giả định `Firebase.initializeApp()` + `useAuthEmulator`/`useFirestoreEmulator` ĐÃ được gọi trước đó ở `main.dart` (Task 3) — KHÔNG gọi lại các hàm init đó trong file này.
    Khai báo credential test cố định ngay đầu hàm với một dòng comment giải thích rõ đây là credential CHỈ dùng cho emulator, không phải tài khoản thật (per Security Domain V2 và threat T-0-02): email `'spike_test_user@example.com'`, password `'SpikeTest123!'`.
    Logic: bọc trong `try/catch`. Gọi `FirebaseAuth.instance.createUserWithEmailAndPassword(email: ..., password: ...)`; nếu ném `FirebaseAuthException` với `code == 'email-already-in-use'` (xảy ra khi bấm nút re-run lần 2 trở đi), fallback sang `FirebaseAuth.instance.signInWithEmailAndPassword(email: ..., password: ...)` bằng cùng credential — để nút "Re-run Firebase Test" không bị fail ở lần chạy thứ hai. Sau khi có user, ghi vào Firestore: `FirebaseFirestore.instance.collection('spike_test').doc('spike_doc').set({'value': 'hello-firebase'})`, rồi đọc lại bằng `.get()`. Trả về chuỗi bắt đầu ĐÚNG với literal `'FIREBASE PASS: '` nối với dữ liệu đọc được.
    Trong khối `catch (e)` bên ngoài, trả về chuỗi bắt đầu ĐÚNG với literal `'FIREBASE FAIL: '` nối với `e`.
  </action>
  <acceptance_criteria>
    - `spike_platform/lib/firebase_service.dart` export hàm `testFirebaseInitAuthFirestore()` kiểu trả về `Future<String>`
    - `grep -c "'FIREBASE PASS: '" spike_platform/lib/firebase_service.dart` trả về ít nhất `1`
    - `grep -c "'FIREBASE FAIL: '" spike_platform/lib/firebase_service.dart` trả về ít nhất `1`
    - `grep -c "spike_test_user@example.com" spike_platform/lib/firebase_service.dart` trả về ít nhất `1`
    - `grep -c "email-already-in-use" spike_platform/lib/firebase_service.dart` trả về ít nhất `1` (fallback signin cho re-run)
    - `cd spike_platform && flutter analyze lib/firebase_service.dart` không báo lỗi
  </acceptance_criteria>
  <verify>
    <automated>cd spike_platform && flutter analyze lib/firebase_service.dart</automated>
  </verify>
  <done>firebase_service.dart tồn tại, xử lý cả signup lần đầu và signin fallback khi re-run, ghi/đọc Firestore, trả về chuỗi PASS/FAIL đúng định dạng.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Viết main.dart — khởi tạo Firebase, trỏ emulator, wire UI tự chạy cả hai test</name>
  <files>spike_platform/lib/main.dart</files>
  <read_first>
    - spike_platform/lib/sqlite_service.dart (vừa tạo ở Task 1 — lấy đúng tên hàm testSqliteInsertAndRead)
    - spike_platform/lib/firebase_service.dart (vừa tạo ở Task 2 — lấy đúng tên hàm testFirebaseInitAuthFirestore)
    - spike_platform/lib/platform_config.dart, spike_platform/lib/firebase_options_spike.dart (Plan 00-01 Task 2)
    - .planning/phases/00-platform-spike/00-RESEARCH.md (mục "Code Examples > Minimal Firebase Initialization for Spike" — dùng làm khung tham chiếu chính xác)
  </read_first>
  <action>
    Viết `main()` với thứ tự BẮT BUỘC: (1) `WidgetsFlutterBinding.ensureInitialized()` phải là dòng thực thi đầu tiên; (2) `await Firebase.initializeApp(options: spikeFirebaseOptions)`; (3) `FirebaseAuth.instance.useAuthEmulator(kAuthEmulatorHost, kAuthEmulatorPort)`; (4) `FirebaseFirestore.instance.useFirestoreEmulator(kFirestoreEmulatorHost, kFirestoreEmulatorPort)`; (5) `runApp(const SpikeApp())`. Import cả 4 file local (`sqlite_service.dart`, `firebase_service.dart`, `platform_config.dart`, `firebase_options_spike.dart`) cùng `package:firebase_core/firebase_core.dart`, `package:firebase_auth/firebase_auth.dart`, `package:cloud_firestore/cloud_firestore.dart`.
    Xây `SpikeApp` (StatelessWidget, `MaterialApp` với `home: SpikeHomePage()`) và `SpikeHomePage` (StatefulWidget). Trong `_SpikeHomePageState`: field `List<String> _log = []`; `initState()` gọi `_runAllTests()` để tự động chạy cả hai test ngay khi màn hình mở (không cần người dùng bấm nút). Hàm `_runAllTests()` gọi tuần tự `testSqliteInsertAndRead()` rồi `testFirebaseInitAuthFirestore()`, mỗi kết quả append vào `_log`, gọi `debugPrint('[SPIKE] $result')` (prefix ĐÚNG `'[SPIKE] '`, có khoảng trắng sau) và `setState(() {})` sau mỗi lần.
    `build()` trả về `Scaffold` với `AppBar` tiêu đề `'Platform Spike'`, một `Row` chứa 2 `ElevatedButton`: nút thứ nhất text ĐÚNG `'Re-run SQLite Test'` gọi lại `testSqliteInsertAndRead()` độc lập; nút thứ hai text ĐÚNG `'Re-run Firebase Test'` gọi lại `testFirebaseInitAuthFirestore()` độc lập — cả hai đều log qua `debugPrint('[SPIKE] $result')` và cập nhật `_log`. Bên dưới là `Expanded(child: ListView(children: _log.map((line) => Text(line)).toList()))` để hiển thị toàn bộ log trên màn hình.
  </action>
  <acceptance_criteria>
    - `spike_platform/lib/main.dart` chứa `WidgetsFlutterBinding.ensureInitialized()` xuất hiện ở dòng nhỏ hơn dòng chứa `Firebase.initializeApp` (thứ tự đúng)
    - `grep -c "Firebase.initializeApp(options: spikeFirebaseOptions)" spike_platform/lib/main.dart` trả về ít nhất `1`
    - `grep -c "useAuthEmulator(kAuthEmulatorHost, kAuthEmulatorPort)" spike_platform/lib/main.dart` trả về ít nhất `1`
    - `grep -c "useFirestoreEmulator(kFirestoreEmulatorHost, kFirestoreEmulatorPort)" spike_platform/lib/main.dart` trả về ít nhất `1`
    - `grep -c "debugPrint('\[SPIKE\] \$" spike_platform/lib/main.dart` trả về ít nhất `2` (mỗi service call site log 1 lần trong `_runAllTests`, cộng 2 lần nữa ở 2 nút re-run)
    - `grep -c "'Re-run SQLite Test'" spike_platform/lib/main.dart` trả về ít nhất `1`
    - `grep -c "'Re-run Firebase Test'" spike_platform/lib/main.dart` trả về ít nhất `1`
    - `cd spike_platform && flutter analyze` (toàn bộ project) thoát exit code 0, không có lỗi
  </acceptance_criteria>
  <verify>
    <automated>cd spike_platform && flutter analyze</automated>
  </verify>
  <done>main.dart khởi tạo Firebase đúng thứ tự, trỏ emulator qua platform_config, tự động chạy cả 2 test khi mở app, log console prefix [SPIKE], UI có đủ 2 nút re-run + danh sách log; flutter analyze toàn project sạch.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Android emulator app (Dart code) → Firebase Auth/Firestore Emulator (10.0.2.2:9099/8080) | Nơi test credential và document test đi qua network loopback |
| spike_platform/lib/*.dart working tree → git remote | Service code không được chứa API key/secret thật trước khi commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-0-02 | Information Disclosure | lib/firebase_service.dart (hardcoded test credential) | accept | Credential `spike_test_user@example.com` / `SpikeTest123!` CHỈ hợp lệ với Auth Emulator cục bộ (không phải project Firebase thật); `spike_platform/` là thư mục throwaway sẽ bị xoá sau Phase 0; comment trong code đánh dấu rõ "emulator-only, not a real account"; Plan 00-03 Task 1 grep lại toàn bộ code trước khi ghi verdict |
| T-0-04 | Information Disclosure | lib/sqlite_service.dart (file spike.db trên storage emulator) | accept | Thư mục `/data/data/<package>/` trên Android là app-private, chỉ app và hệ thống đọc được; không có dữ liệu người dùng thật trong spike, không cần thêm mitigation |
</threat_model>

<verification>
- `cd spike_platform && flutter analyze` toàn project thoát exit code 0
- `grep -c "sqflite_common_ffi\|databaseFactoryFfi" spike_platform/lib/*.dart` trả về `0` cho mọi file
- 3 file `sqlite_service.dart`, `firebase_service.dart`, `main.dart` đều tồn tại và export đúng hàm theo `<interfaces>`
</verification>

<success_criteria>
`spike_platform/` build sạch (`flutter analyze` không lỗi), app khi chạy sẽ tự động thực thi cả 2 test khi khởi động và log ra console đúng format `[SPIKE] SQLITE PASS/FAIL: ...` và `[SPIKE] FIREBASE PASS/FAIL: ...`, UI hiển thị đủ 2 nút re-run. Sẵn sàng cho Plan 00-03 chạy verification thật trên emulator.
</success_criteria>

<output>
Create `.planning/phases/00-platform-spike/00-02-SUMMARY.md` when done
</output>
