---
phase: 00-platform-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spike_platform/pubspec.yaml
  - spike_platform/.gitignore
  - spike_platform/android/app/build.gradle
  - spike_platform/android/build.gradle
  - spike_platform/android/settings.gradle
  - spike_platform/android/app/src/debug/AndroidManifest.xml
  - spike_platform/android/app/google-services.json
  - spike_platform/lib/platform_config.dart
  - spike_platform/lib/firebase_options_spike.dart
  - spike_platform/firebase.json
  - spike_platform/firestore.rules
  - spike_platform/.firebaserc
autonomous: true
requirements: [FND-01, FND-04]

must_haves:
  truths:
    - "spike_platform project compiles with pinned firebase_core 4.12.1, firebase_auth 6.5.6, cloud_firestore 6.7.1, sqflite 2.4.3 dependencies (flutter pub get exits 0)"
    - "Android build config enforces minSdkVersion >= 23 and enables cleartext traffic only in the debug build variant, never release"
    - "google-services.json exists on disk for Gradle to consume but is never committed to git"
    - "No real Firebase API key (AIza* prefix) appears anywhere under spike_platform/lib/"
  artifacts:
    - path: "spike_platform/pubspec.yaml"
      provides: "Pinned Firebase + sqflite dependency versions"
      contains: "firebase_core"
    - path: "spike_platform/android/app/build.gradle"
      provides: "minSdkVersion 23 + google-services plugin applied"
      contains: "minSdkVersion 23"
    - path: "spike_platform/android/app/src/debug/AndroidManifest.xml"
      provides: "Debug-only cleartext traffic permission for loopback emulator access"
      contains: "usesCleartextTraffic"
    - path: "spike_platform/lib/platform_config.dart"
      provides: "Platform-conditional emulator host/port constants"
      exports: ["kAuthEmulatorHost", "kAuthEmulatorPort", "kFirestoreEmulatorHost", "kFirestoreEmulatorPort"]
    - path: "spike_platform/lib/firebase_options_spike.dart"
      provides: "Dummy emulator-only FirebaseOptions (no real API key)"
      exports: ["spikeFirebaseOptions"]
    - path: "spike_platform/firebase.json"
      provides: "Firebase Emulator Suite port configuration"
      contains: "emulators"
    - path: "spike_platform/firestore.rules"
      provides: "Permissive emulator-only Firestore rules baseline"
      contains: "allow read, write: if true;"
  key_links:
    - from: "spike_platform/android/settings.gradle"
      to: "spike_platform/android/app/google-services.json"
      via: "com.google.gms.google-services Gradle plugin declared in plugins DSL"
      pattern: "com\\.google\\.gms\\.google-services"
    - from: "spike_platform/lib/platform_config.dart"
      to: "spike_platform/lib/firebase_options_spike.dart"
      via: "both consumed together by Firebase init in main.dart (Plan 00-02)"
      pattern: "kAuthEmulatorHost|kFirestoreEmulatorHost"
---

<objective>
Dựng khung dự án Flutter throwaway `spike_platform/` (Android-only), pin đúng version dependency theo CLAUDE.md, và cấu hình phần Android/Firebase Emulator cần thiết để hai service ở Plan 00-02 có thể build và chạy được trên Android emulator.

Purpose: Đây là nền tảng bắt buộc trước khi viết bất kỳ logic SQLite/Firebase nào — nếu Gradle config, minSdkVersion, cleartext traffic, hay dummy Firebase options sai, mọi thứ ở Plan 00-02/00-03 sẽ fail ngay từ bước build.
Output: `spike_platform/` scaffold Android-only đã pub get thành công, config Android đúng minSdk 23 + cleartext debug-only, dummy Firebase config (không có API key thật), Firebase Emulator Suite config (`firebase.json`, `firestore.rules` permissive).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/00-platform-spike/00-RESEARCH.md
@.planning/phases/00-platform-spike/00-VALIDATION.md
@CLAUDE.md
</context>

<interfaces>
<!-- Không có code có sẵn trong repo (greenfield). Các giá trị dưới đây là hợp đồng mà Plan 00-02 sẽ tiêu thụ trực tiếp — không cần dò lại codebase. -->

Từ 00-RESEARCH.md Pattern 1 (Platform-Conditional Emulator Constants), file `lib/platform_config.dart` PHẢI export:
```
const int kAuthEmulatorPort = 9099;
const int kFirestoreEmulatorPort = 8080;
String get kAuthEmulatorHost => Platform.isAndroid ? '10.0.2.2' : 'localhost';
String get kFirestoreEmulatorHost => Platform.isAndroid ? '10.0.2.2' : 'localhost';
```

Từ 00-RESEARCH.md Pattern 2, `lib/firebase_options_spike.dart` PHẢI export một hằng `spikeFirebaseOptions` kiểu `FirebaseOptions` (từ `package:firebase_core/firebase_core.dart`) mà Plan 00-02 Task 3 sẽ truyền vào `Firebase.initializeApp(options: spikeFirebaseOptions)`.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold spike_platform và pin dependency</name>
  <files>spike_platform/pubspec.yaml, spike_platform/.gitignore</files>
  <read_first>
    - .planning/phases/00-platform-spike/00-RESEARCH.md (mục "Standard Stack" và "Installation")
    - CLAUDE.md (bảng Technology Stack — version pin chính xác)
  </read_first>
  <action>
    Từ thư mục gốc repo, chạy `flutter create --platforms=android --org com.memocard.spike spike_platform` để sinh khung Flutter Android-only (KHÔNG thêm platform nào khác — không windows, không ios, không web, không linux, không macos).
    Trong `spike_platform/pubspec.yaml`, thêm đúng các dependency version pin theo CLAUDE.md: `firebase_core: ^4.12.1`, `firebase_auth: ^6.5.6`, `cloud_firestore: ^6.7.1`, `sqflite: ^2.4.3`. KHÔNG thêm `sqflite_common_ffi` dưới bất kỳ hình thức nào (per RESEARCH.md Pitfall 6 — package này chỉ cần cho Windows/Linux desktop, đã bị loại khỏi scope).
    Trong `spike_platform/.gitignore` (file mặc định do `flutter create` sinh ra), thêm dòng mới `android/app/google-services.json` để đảm bảo file config Firebase không bao giờ bị commit (per Security Domain — Disclosure risk T-0-03).
    Chạy `cd spike_platform && flutter pub get` để resolve dependency, xác nhận không có lỗi resolve version.
  </action>
  <acceptance_criteria>
    - `spike_platform/pubspec.yaml` chứa các dòng: `firebase_core: ^4.12.1`, `firebase_auth: ^6.5.6`, `cloud_firestore: ^6.7.1`, `sqflite: ^2.4.3`
    - `grep -c "sqflite_common_ffi" spike_platform/pubspec.yaml` trả về `0`
    - `spike_platform/.gitignore` chứa chuỗi `google-services.json`
    - `spike_platform/android/` tồn tại; `spike_platform/windows/`, `spike_platform/linux/`, `spike_platform/macos/`, `spike_platform/web/`, `spike_platform/ios/` KHÔNG tồn tại
    - `flutter pub get` (chạy trong `spike_platform/`) thoát với exit code 0
  </acceptance_criteria>
  <verify>
    <automated>cd spike_platform && flutter pub get</automated>
  </verify>
  <done>pubspec.yaml pin đúng 4 package bắt buộc, không có sqflite_common_ffi, flutter pub get thành công, chỉ có target Android.</done>
</task>

<task type="auto">
  <name>Task 2: Cấu hình Android (minSdk, cleartext debug, Gradle google-services plugin) + dummy Firebase config</name>
  <files>spike_platform/android/app/build.gradle, spike_platform/android/build.gradle, spike_platform/android/settings.gradle, spike_platform/android/app/src/debug/AndroidManifest.xml, spike_platform/android/app/google-services.json, spike_platform/lib/platform_config.dart, spike_platform/lib/firebase_options_spike.dart</files>
  <read_first>
    - spike_platform/android/app/build.gradle (file vừa được flutter create sinh ra ở Task 1)
    - spike_platform/android/settings.gradle (file vừa được flutter create sinh ra ở Task 1)
    - .planning/phases/00-platform-spike/00-RESEARCH.md (mục "Pattern 1", "Pattern 2", "Pitfall 2", "Pitfall 3", "Pitfall 5")
  </read_first>
  <action>
    Trong `android/app/build.gradle`, đặt `minSdkVersion 23` tường minh trong khối `defaultConfig` (per RESEARCH Pitfall 3 — Firebase yêu cầu tối thiểu API 23; không dùng `flutter.minSdkVersion` mặc định vì có thể thấp hơn).
    Wiring Google Services Gradle plugin theo declarative plugins DSL (per RESEARCH "State of the Art"): trong `android/settings.gradle`, thêm vào khối `plugins { ... }` dòng `id "com.google.gms.google-services" version "4.4.2" apply false`; trong `android/app/build.gradle`, thêm vào khối `plugins { ... }` ở đầu file dòng `id "com.google.gms.google-services"` (không dùng cú pháp `apply plugin:` cũ).
    Tạo file `android/app/src/debug/AndroidManifest.xml` mới (thư mục `debug/` chưa tồn tại, phải tạo) chứa một `<application>` tag với thuộc tính `android:usesCleartextTraffic="true"` — CHỈ áp dụng cho debug variant, không bao giờ đụng tới `android/app/src/main/AndroidManifest.xml` hay bất kỳ release config nào (per RESEARCH Pitfall 2 và Security Domain V6).
    Tạo file dummy `android/app/google-services.json` với cấu trúc JSON hợp lệ tối thiểu để Gradle plugin không báo lỗi thiếu file: `project_info.project_id` = `"demo-spike-project"`, `client[0].client_info.android_client_info.package_name` = `"com.memocard.spike"`. Giá trị `api_key[0].current_key` đặt là chuỗi rõ ràng KHÔNG THẬT và KHÔNG bắt đầu bằng tiền tố `AIza` (ví dụ `"no-real-key-emulator-only-dummy"`) — vì Firebase init thực tế dùng `FirebaseOptions` truyền tay ở Dart layer (xem dưới), file JSON này chỉ để thoả điều kiện build của Gradle plugin.
    Tạo `lib/platform_config.dart` đúng theo hợp đồng ở `<interfaces>` phía trên (import `dart:io`, export `kAuthEmulatorPort = 9099`, `kFirestoreEmulatorPort = 8080`, `kAuthEmulatorHost`/`kFirestoreEmulatorHost` trả về `10.0.2.2` khi `Platform.isAndroid`, ngược lại `localhost`).
    Tạo `lib/firebase_options_spike.dart` export hằng `spikeFirebaseOptions` kiểu `FirebaseOptions` (import từ `package:firebase_core/firebase_core.dart`) với các field: `apiKey: 'demo-api-key-not-a-real-key'` (KHÔNG bắt đầu bằng `AIza` — tránh false positive ở bước grep credential-hygiene tại Plan 00-03), `appId: '1:000000000000:android:0000000000000000'`, `messagingSenderId: '000000000000'`, `projectId: 'demo-spike-project'`, `storageBucket: 'demo-spike-project.appspot.com'`.
  </action>
  <acceptance_criteria>
    - `grep -c "minSdkVersion 23" spike_platform/android/app/build.gradle` trả về ít nhất `1`
    - `grep -c "com.google.gms.google-services" spike_platform/android/settings.gradle` trả về ít nhất `1`
    - `grep -c "com.google.gms.google-services" spike_platform/android/app/build.gradle` trả về ít nhất `1`
    - `spike_platform/android/app/src/debug/AndroidManifest.xml` tồn tại và chứa chuỗi `usesCleartextTraffic="true"`
    - `grep -c "usesCleartextTraffic" spike_platform/android/app/src/main/AndroidManifest.xml` trả về `0` (cleartext KHÔNG được thêm vào main manifest)
    - `spike_platform/android/app/google-services.json` tồn tại trên đĩa; `git check-ignore spike_platform/android/app/google-services.json` thoát exit code `0` (xác nhận bị git bỏ qua)
    - `spike_platform/lib/platform_config.dart` export đúng 4 identifier: `kAuthEmulatorHost`, `kAuthEmulatorPort`, `kFirestoreEmulatorHost`, `kFirestoreEmulatorPort`
    - `spike_platform/lib/firebase_options_spike.dart` export `spikeFirebaseOptions`; `grep -c "AIza" spike_platform/lib/firebase_options_spike.dart` trả về `0`
  </acceptance_criteria>
  <verify>
    <automated>cd spike_platform && flutter analyze lib/platform_config.dart lib/firebase_options_spike.dart</automated>
  </verify>
  <done>Android build config đúng minSdk 23 + cleartext debug-only + Gradle google-services plugin wired; dummy Firebase config không chứa API key thật ở bất kỳ đâu trong lib/.</done>
</task>

<task type="auto">
  <name>Task 3: Cấu hình Firebase Emulator Suite + xác minh firebase-tools CLI</name>
  <files>spike_platform/firebase.json, spike_platform/firestore.rules, spike_platform/.firebaserc</files>
  <read_first>
    - .planning/phases/00-platform-spike/00-RESEARCH.md (mục "Open Questions" #1, #2, #3, mục "Common Pitfalls > Pitfall 4")
  </read_first>
  <action>
    Chạy `firebase --version` để kiểm tra `firebase-tools` đã cài global chưa; nếu lệnh không tồn tại (command not found), chạy `npm install -g firebase-tools` rồi chạy lại `firebase --version` để xác nhận (per RESEARCH Package Legitimacy Audit — firebase-tools đã được duyệt Approved, an toàn cài global, KHÔNG dùng `npx firebase-tools`).
    Tạo `spike_platform/.firebaserc` khai báo `{"projects": {"default": "demo-spike-project"}}`.
    Tạo `spike_platform/firebase.json` khai báo khối `"emulators"` với `"auth": {"port": 9099}`, `"firestore": {"port": 8080}`, `"ui": {"enabled": true, "port": 4000}`, và `"firestore": {"rules": "firestore.rules"}` ở khối cấu hình Firestore gốc (ngoài `emulators`) trỏ tới file rules bên dưới. KHÔNG thêm bất kỳ khối `"emulators": {"import"/"export"}` nào — nghị quyết Open Question #3 (researcher): emulator luôn khởi động với dữ liệu trống (start fresh), không seed baseline dataset ở Phase 0; import/export baseline (nếu cần) sẽ do Phase 1 quyết định.
    Tạo `spike_platform/firestore.rules` với nội dung permissive-only-cho-emulator (nghị quyết Open Question #2 của researcher — KHÔNG thiết kế production rules ở phase này, Phase 1 sẽ làm việc đó): `rules_version = '2';` rồi `service cloud.firestore { match /databases/{database}/documents { match /{document=**} { allow read, write: if true; } } }`.
    Ghi chú quyết định cho Open Question #1: researcher khuyến nghị khởi động `firebase emulators:start` thủ công; planner đây QUYẾT ĐỊNH lệch khỏi khuyến nghị đó — dự án dùng `workflow.human_verify_mode = end-of-phase` (không có mid-flight checkpoint), nên theo nguyên tắc Automation-first, Plan 00-03 Task 2 sẽ để Claude TỰ ĐỘNG khởi động và tắt `firebase emulators:start` bằng Bash (background process + poll log), thay vì yêu cầu con người mở terminal thủ công. Lý do: tránh cold-start cost của một checkpoint mid-flight khi hoàn toàn có thể tự động hoá qua CLI.
  </action>
  <acceptance_criteria>
    - `firebase --version` thoát exit code 0 và in ra một chuỗi version (vd. `13.x.x`)
    - `spike_platform/firebase.json` chứa key `"emulators"` với `"auth"` port `9099` và `"firestore"` port `8080`
    - `grep -c "import\|export" spike_platform/firebase.json` trả về `0` (không cấu hình seed/export data — start fresh)
    - `spike_platform/firestore.rules` chứa chuỗi `allow read, write: if true;`
    - `spike_platform/.firebaserc` chứa chuỗi `demo-spike-project`
  </acceptance_criteria>
  <verify>
    <automated>firebase --version</automated>
  </verify>
  <done>Firebase Emulator Suite config sẵn sàng (port 9099 auth, 8080 firestore, 4000 ui), firestore.rules permissive cho emulator, không seed data, firebase-tools CLI xác minh hoạt động.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Android emulator app → Firebase Emulator Suite (host loopback 10.0.2.2) | App gửi request Auth/Firestore không mã hoá (cleartext) tới máy dev qua loopback đặc biệt của Android emulator |
| Developer machine → npm/pub registries | Cài `firebase-tools` (npm) và resolve `firebase_core`/`firebase_auth`/`cloud_firestore`/`sqflite` (pub.dev) |
| spike_platform/ repo working tree → git remote | `google-services.json` và `firebase_options_spike.dart` phải không chứa secret thật trước khi commit |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-0-01 | Tampering | android/app/src/debug/AndroidManifest.xml (cleartext traffic) | mitigate | `usesCleartextTraffic="true"` CHỈ đặt trong `src/debug/AndroidManifest.xml`, không đụng `src/main/` hay release config; loopback 10.0.2.2 chỉ truy cập được từ chính emulator, không expose ra mạng ngoài |
| T-0-03 | Information Disclosure | android/app/google-services.json, lib/firebase_options_spike.dart | mitigate | `google-services.json` nằm trong `.gitignore` (không commit); `firebase_options_spike.dart` dùng giá trị dummy không bắt đầu bằng `AIza`, xác minh bằng grep ở Plan 00-03 Task 1 |
| T-0-06 | Integrity | android/app/build.gradle (minSdkVersion) | mitigate | Đặt tường minh `minSdkVersion 23` — Firebase SDK yêu cầu tối thiểu API 23, Gradle sẽ reject build nếu thấp hơn thay vì fail âm thầm lúc runtime |
| T-0-SC | Tampering | npm install -g firebase-tools; pub.dev firebase_core/firebase_auth/cloud_firestore/sqflite | accept | Toàn bộ 5 package trong Package Legitimacy Audit (00-RESEARCH.md) đều có disposition Approved, slopcheck [OK], không có package [ASSUMED]/[SUS]/[SLOP] — không cần checkpoint chặn cài đặt |
</threat_model>

<verification>
- `cd spike_platform && flutter pub get` exit code 0
- `cd spike_platform && flutter analyze lib/platform_config.dart lib/firebase_options_spike.dart` không có lỗi
- `firebase --version` thoát exit code 0
- `git check-ignore spike_platform/android/app/google-services.json` thoát exit code 0
- `grep -rn "sqflite_common_ffi\|databaseFactoryFfi" spike_platform/` không có kết quả
</verification>

<success_criteria>
`spike_platform/` build được (Gradle sync sạch), pin đúng 4 dependency bắt buộc, minSdk 23 + cleartext debug-only, google-services.json gitignored, Firebase Emulator config (9099/8080/4000) + firestore.rules permissive sẵn sàng cho Plan 00-02/00-03.
</success_criteria>

<output>
Create `.planning/phases/00-platform-spike/00-01-SUMMARY.md` when done
</output>
