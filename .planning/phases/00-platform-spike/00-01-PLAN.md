---
phase: 00-platform-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - spike_platform/pubspec.yaml
  - spike_platform/lib/platform_config.dart
  - spike_platform/lib/firebase_options_spike.dart
  - spike_platform/firebase.json
  - spike_platform/firestore.rules
autonomous: false
requirements: [FND-04]
user_setup:
  - service: firebase-cli
    why: "The Firebase Emulator Suite (Auth + Firestore) is started via the `firebase` CLI. There is no programmatic install path Claude can run unattended and be certain PATH is refreshed for the current shell."
    env_vars: []
    dashboard_config:
      - task: "Install the Firebase CLI globally (`npm install -g firebase-tools`) after verifying it is the official package at https://www.npmjs.com/package/firebase-tools, then confirm `firebase --version` prints a version string."
        location: "Local terminal (PowerShell or Git Bash)"

must_haves:
  truths:
    - "`flutter create` succeeds and spike_platform/ is a valid Flutter project targeting windows and android"
    - "Firebase CLI is installed and reachable via `firebase --version`"
  artifacts:
    - path: "spike_platform/pubspec.yaml"
      provides: "Pinned Firebase + SQLite dependencies for the throwaway spike"
      contains: "firebase_core"
    - path: "spike_platform/lib/platform_config.dart"
      provides: "Platform-conditional emulator host/port constants (Android 10.0.2.2 vs Windows localhost)"
      exports: ["kAuthEmulatorHost", "kFirestoreEmulatorHost", "kAuthEmulatorPort", "kFirestoreEmulatorPort"]
    - path: "spike_platform/lib/firebase_options_spike.dart"
      provides: "Hand-written dummy FirebaseOptions for emulator-only init (no flutterfire configure, no real project)"
      exports: ["spikeFirebaseOptions"]
    - path: "spike_platform/firebase.json"
      provides: "Firebase Emulator Suite config (auth 9099, firestore 8080, ui 4000)"
      contains: "emulators"
  key_links:
    - from: "spike_platform/pubspec.yaml"
      to: "pub.dev packages"
      via: "flutter pub get dependency resolution"
      pattern: "firebase_core|sqflite_common_ffi"
---

## Phase Goal

> **Note:** ROADMAP.md's Phase 0 `**Goal:**` line is written as a technical outcome statement, not yet in `As a/I want to/so that` user-story format (this phase predates a `/gsd mvp-phase 0` pass). The framing below is synthesized directly from the ROADMAP goal and the orchestrator's phase intent — it is not invented — and is offered here so MVP-mode vertical-slice conventions still apply. Consider running `/gsd mvp-phase 0` to formalize this in ROADMAP.md.

**As a** developer about to build 8 phases of production code on this stack, **I want to** see proof that Firebase (Auth + Firestore) and `sqflite_common_ffi` actually initialize and perform real read/write work on both an Android emulator and Windows desktop, **so that** the team does not commit weeks of work to a stack assumption that silently fails on one platform.

<objective>
Scaffold the throwaway spike Flutter project (`spike_platform/`, isolated from the repo root so Phase 1's real `flutter create` never collides with it), declare the pinned Firebase + SQLite dependencies from CLAUDE.md, write the platform-conditional emulator constants and dummy `FirebaseOptions` contract that later plans implement against, and confirm the Firebase CLI is present so the Emulator Suite can be started in later waves.

Purpose: Everything downstream (SQLite service, Firebase service, UI, both platform runs) depends on this scaffold existing and resolving cleanly. Getting the dependency versions and emulator config right here, once, avoids repeating discovery work in every later plan.
Output: A buildable `spike_platform/` Flutter project (Windows + Android targets) with pinned dependencies resolved, and a confirmed-working Firebase CLI on this machine.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@./CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold spike_platform Flutter project with pinned dependencies</name>
  <files>spike_platform/pubspec.yaml</files>
  <read_first>
    - CLAUDE.md — "Technology Stack" section, table rows for firebase_core, firebase_auth, cloud_firestore, sqflite, sqflite_common_ffi (exact pinned versions and Windows/Android platform support matrix)
    - .planning/STATE.md — "Blockers/Concerns" section (Firebase Windows support is MEDIUM confidence, this spike exists to resolve that)
  </read_first>
  <action>
    From the repo root (C:\Users\LENOVO\AndroidStudioProjects\prm_proejct), run `flutter doctor -v` first and capture the output for the record (do not block on warnings unrelated to windows/android). Then run `flutter create --platforms=windows,android --org com.memocard.spike spike_platform` from the repo root. This creates ./spike_platform/ as a new, separate Flutter project — do NOT run flutter create at the repo root itself, and do not touch any existing repo files outside spike_platform/. Edit the generated spike_platform/pubspec.yaml: set `description: Throwaway platform-spike for Phase 0 -- DO NOT build Phase 1 on this directory.` and add these exact dependency entries under `dependencies:` (below the existing `flutter:` entry): `firebase_core: ^4.12.1`, `firebase_auth: ^6.5.6`, `cloud_firestore: ^6.7.1`, `sqflite: ^2.4.3`, `sqflite_common_ffi: ^2.4.2`. Run `flutter pub get` from spike_platform/. CLAUDE.md's versions are researched claims, not verified facts — do not edit pubspec.lock by hand; let pub resolve it.
  </action>
  <acceptance_criteria>
    - `flutter create` exit code 0 and spike_platform/lib/main.dart, spike_platform/windows/, spike_platform/android/ all exist
    - `flutter pub get` exit code 0 with no dependency resolution errors printed to stdout/stderr
    - spike_platform/pubspec.lock exists and contains a `firebase_core:` package block with a `version:` line (the actually-resolved version, to be recorded verbatim in the Phase 05 findings doc — do not assume it matches CLAUDE.md's ^4.12.1 claim)
  </acceptance_criteria>
  <verify>
    <automated>cd spike_platform && flutter pub get && grep -A2 "^  firebase_core:" pubspec.lock | grep -c "version:"</automated>
  </verify>
  <done>spike_platform/ is a valid Flutter project (windows + android platforms present) with all 5 pinned packages resolved in pubspec.lock.</done>
</task>

<task type="auto">
  <name>Task 2: Write platform-conditional emulator constants, dummy FirebaseOptions, and firebase.json emulator config</name>
  <files>spike_platform/lib/platform_config.dart, spike_platform/lib/firebase_options_spike.dart, spike_platform/firebase.json, spike_platform/firestore.rules</files>
  <read_first>
    - spike_platform/pubspec.yaml (created in Task 1 — confirms firebase_core package name/version available for import)
  </read_first>
  <action>
    Create spike_platform/lib/platform_config.dart: import `dart:io` for `Platform`. Define `const int kAuthEmulatorPort = 9099;` and `const int kFirestoreEmulatorPort = 8080;`. Define two getters, `String get kAuthEmulatorHost` and `String get kFirestoreEmulatorHost`, both returning `Platform.isAndroid ? '10.0.2.2' : 'localhost'` — this is the Android-emulator-to-host-loopback address (10.0.2.2) vs Windows-desktop-to-localhost address, per the user's locked decision. Create spike_platform/lib/firebase_options_spike.dart: import `package:firebase_core/firebase_core.dart`, export a single `const FirebaseOptions spikeFirebaseOptions = FirebaseOptions(...)` with exactly these dummy values: `apiKey: 'demo-api-key'`, `appId: '1:000000000000:android:0000000000000000'`, `messagingSenderId: '000000000000'`, `projectId: 'demo-spike-project'`. Add a doc comment directly above the const stating: "Dummy values only — valid ONLY against the Firebase Local Emulator Suite with project id demo-spike-project. NEVER replace with real credentials in this throwaway spike." Create spike_platform/firebase.json at spike_platform/ root with an `emulators` object containing `auth: {port: 9099}`, `firestore: {port: 8080}`, `ui: {enabled: true, port: 4000}`. Create spike_platform/firestore.rules with `rules_version = '2';` and a `service cloud.firestore` block that matches `/databases/{database}/documents` and `/{document=**}` with `allow read, write: if true;` — this open rule is acceptable ONLY because it targets the local emulator with dummy project id demo-spike-project, never a real deployed project (see plan threat_model T-00-03).
  </action>
  <acceptance_criteria>
    - `grep -c "kAuthEmulatorHost" spike_platform/lib/platform_config.dart` returns 1 or more, and the file contains both `10.0.2.2` and `localhost` literal strings
    - `grep -c "spikeFirebaseOptions" spike_platform/lib/firebase_options_spike.dart` returns 1 or more, and the file contains the literal string `demo-api-key` (not any string starting with `AIza`, which is the real Google API key prefix)
    - spike_platform/firebase.json is valid JSON (parses without error) and contains keys `auth`, `firestore`, `ui` under `emulators`
    - `dart analyze spike_platform/lib/platform_config.dart spike_platform/lib/firebase_options_spike.dart` reports no errors (warnings acceptable)
  </acceptance_criteria>
  <verify>
    <automated>cd spike_platform && dart analyze lib/platform_config.dart lib/firebase_options_spike.dart 2>&1 | grep -v "^Analyzing" ; node -e "JSON.parse(require('fs').readFileSync('firebase.json','utf8')); console.log('firebase.json OK')"</automated>
  </verify>
  <done>Both Dart contract files exist, analyze cleanly, and contain only dummy/emulator-safe values; firebase.json and firestore.rules exist and are syntactically valid.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 3: Verify Firebase CLI is installed (npm legitimacy check + install if missing)</name>
  <files>none (tooling verification only)</files>
  <read_first>
    - This plan's `user_setup` frontmatter block above (states why this cannot be fully automated)
  </read_first>
  <action>
    Run `firebase --version` in a terminal. If it prints a version string (e.g. `13.x.x`), this task is already satisfied — report the version and continue. If the command is not found: (1) run `node --version` and `npm --version` to confirm Node.js/npm are available (Firebase CLI requires them); (2) before installing, visit https://www.npmjs.com/package/firebase-tools and confirm it is the official Firebase CLI package published under the `firebase` npm org with a high weekly download count — this is the supply-chain legitimacy check for T-00-SC below; (3) run `npm install -g firebase-tools`; (4) re-run `firebase --version` to confirm the install succeeded. Report the final `firebase --version` output back before this task is marked resumed.
  </action>
  <acceptance_criteria>
    - `firebase --version` exits 0 and prints a semantic version string
    - If an install was required, the human confirms https://www.npmjs.com/package/firebase-tools was checked and matches the official Firebase CLI before running `npm install -g firebase-tools`
  </acceptance_criteria>
  <verify>
    <automated>firebase --version</automated>
  </verify>
  <what-built>Nothing new — this task verifies a prerequisite tool the later Wave 3/4 plans depend on (`firebase emulators:start`).</what-built>
  <how-to-verify>
    Run `firebase --version` in your terminal. If it errors with "command not found", follow the install steps in &lt;action&gt; above, then re-run it.
  </how-to-verify>
  <resume-signal>Reply with the exact `firebase --version` output once it succeeds.</resume-signal>
  <done>`firebase --version` succeeds locally and its output has been recorded for the Phase 05 findings doc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Developer machine ↔ pub.dev | `flutter pub get` resolves and downloads Dart packages declared in pubspec.yaml |
| Developer machine ↔ npm registry | `npm install -g firebase-tools` downloads and executes the Firebase CLI globally |
| Local files ↔ git | spike_platform/ config files (firebase.json, firestore.rules, firebase_options_spike.dart) will be committed to the shared repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-00-01 | Tampering | pubspec.yaml dependency resolution | mitigate | Use exact pinned versions from CLAUDE.md's researched stack table, not arbitrary `any` ranges; verify resolved versions in pubspec.lock match a legitimate published release (not a typosquat) by cross-checking package name spelling against pub.dev in Task 1 |
| T-00-SC | Tampering | `npm install -g firebase-tools` (supply chain) | mitigate | Blocking human checkpoint (Task 3) requires visiting npmjs.com/package/firebase-tools and confirming it is the official Firebase CLI before installing; no [SLOP]/unverified package is auto-installed |
| T-00-02 | Information Disclosure | spike_platform/lib/firebase_options_spike.dart | mitigate | Only dummy `demo-`/placeholder values are ever written (never a real apiKey); re-verified via grep in Plan 05 Task 2 before the phase is considered complete |
| T-00-03 | Information Disclosure | spike_platform/firestore.rules (`allow read, write: if true`) | accept | Rules apply only to the local Firestore Emulator bound to loopback with dummy project id `demo-spike-project` — no network exposure, no real data, not a deployed production project |

</threat_model>

<verification>
- `spike_platform/` exists as a valid Flutter project with windows + android platform folders
- `spike_platform/pubspec.lock` contains resolved versions for all 5 pinned packages
- `spike_platform/lib/platform_config.dart` and `spike_platform/lib/firebase_options_spike.dart` analyze cleanly and contain only dummy/emulator-safe values
- `firebase --version` succeeds on this machine
</verification>

<success_criteria>
Plan is complete when: flutter pub get succeeds with all 5 pinned dependencies resolved, the platform-config and firebase-options contract files exist and analyze cleanly, firebase.json/firestore.rules are valid, and the Firebase CLI is confirmed installed and working.
</success_criteria>

<output>
Create `.planning/phases/00-platform-spike/00-01-SUMMARY.md` when done
</output>
