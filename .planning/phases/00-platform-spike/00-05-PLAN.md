---
phase: 00-platform-spike
plan: 05
type: execute
wave: 5
depends_on: ["00-03", "00-04"]
files_modified:
  - .planning/phases/00-platform-spike/00-SPIKE-FINDINGS.md
  - spike_platform/.gitignore
autonomous: true
requirements: [FND-01, FND-02, FND-03, FND-04]

must_haves:
  truths:
    - "A findings document exists recording pass/fail per FND-01..04 per platform, the actually-resolved package versions, and an explicit GO / GO-WITH-CAVEATS / NO-GO verdict"
    - "No real Firebase credentials are committed anywhere in spike_platform/"
  artifacts:
    - path: ".planning/phases/00-platform-spike/00-SPIKE-FINDINGS.md"
      provides: "Phase 0 verdict, consumed by Phase 1 before it starts"
      contains: "Verdict"
  key_links:
    - from: ".planning/phases/00-platform-spike/00-SPIKE-FINDINGS.md"
      to: "spike_platform/pubspec.lock"
      via: "resolved version numbers copied verbatim from pubspec.lock into the findings table"
      pattern: "firebase_core"
    - from: ".planning/phases/00-platform-spike/00-SPIKE-FINDINGS.md"
      to: "spike_platform/windows_run_log.txt and spike_platform/android_run_log.txt"
      via: "PASS/FAIL lines and any captured error text copied into the findings per-criterion table"
      pattern: "SPIKE"
---

## Phase Goal

**As a** developer about to build 8 phases of production code on this stack, **I want to** see proof that Firebase (Auth + Firestore) and `sqflite_common_ffi` actually initialize and perform real read/write work on both an Android emulator and Windows desktop, **so that** the team does not commit weeks of work to a stack assumption that silently fails on one platform.

<objective>
Synthesize the evidence captured in Plans 03 and 04 into a single findings document with an explicit go/no-go verdict, and do a final credential-hygiene pass on the throwaway spike code before considering the phase closed. This is the terminal task of Phase 0 — Phase 1 reads this document before deciding whether to proceed as planned or fall back (REST API for Auth/Firestore, Android-only demo, or renegotiating the Windows requirement, per STATE.md's documented fallbacks).

Purpose: The whole value of a spike is the verdict with evidence, not the throwaway code itself. This plan is where that value gets written down.
Output: .planning/phases/00-platform-spike/00-SPIKE-FINDINGS.md
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the SPIKE-FINDINGS.md verdict document</name>
  <files>.planning/phases/00-platform-spike/00-SPIKE-FINDINGS.md</files>
  <read_first>
    - spike_platform/pubspec.lock (Plan 01 output — actually-resolved package versions)
    - spike_platform/windows_run_log.txt (Plan 03 output — Windows evidence)
    - spike_platform/android_run_log.txt (Plan 04 output — Android evidence)
    - spike_platform/firebase_emulator_windows.log and spike_platform/firebase_emulator_android.log (Plans 03/04 output — emulator boot evidence)
    - .planning/STATE.md — "Blockers/Concerns" section (documents the 3 fallback options if Firebase Windows support fails: REST API for auth/Firestore, Android-only demo, or renegotiating the Windows requirement)
  </read_first>
  <action>
    Grep spike_platform/pubspec.lock for the resolved `version:` lines of firebase_core, firebase_auth, cloud_firestore, sqflite, and sqflite_common_ffi. Grep windows_run_log.txt and android_run_log.txt for all `[SPIKE] SQLITE` and `[SPIKE] FIREBASE` lines (including full FAIL error text if present). Write .planning/phases/00-platform-spike/00-SPIKE-FINDINGS.md with these exact sections, in order: (1) `## Verdict` — one of `GO`, `GO-WITH-CAVEATS`, or `NO-GO`, chosen per this rule: GO if all 4 criteria (FND-01..04) passed on both platforms; GO-WITH-CAVEATS if FND-01/02 (launch) passed on both but at least one of FND-03/04 failed on exactly one platform with a clear fix path visible in the captured error; NO-GO if any criterion failed on both platforms, or a launch criterion (FND-01/FND-02) failed outright. (2) `## Per-Criterion Results` — a table with columns Criterion, Android, Windows, Evidence, containing one row each for FND-01, FND-02, FND-03, FND-04, each Android/Windows cell either PASS or FAIL, Evidence column quoting the relevant `[SPIKE]` log line or emulator log excerpt. (3) `## Actually Resolved Package Versions` — a table with columns Package, CLAUDE.md Claimed Version, Actually Resolved Version (Windows), Actually Resolved Version (Android) — noting explicitly that pub resolves per-platform if versions differ, or a single shared version if pubspec.lock is not platform-specific (Flutter's pub resolution is platform-agnostic per project, so this will likely be one column of actual values — state this explicitly rather than assuming). (4) `## Errors Encountered and Fixes` — a bulleted list of every distinct error string seen across all captured logs (Task 1-4 emulator logs, run logs, `flutter doctor` output), each with either the fix that was applied or "unresolved — needs investigation before Phase 1" if no fix was found. (5) `## Fallback Recommendation` — only populated if Verdict is not GO: reference the specific fallback(s) from STATE.md's Blockers/Concerns section (REST API for Auth/Firestore, Android-only demo, or renegotiating the Windows requirement) that best fit the actual failure observed, with reasoning tied to the specific error captured.
  </action>
  <acceptance_criteria>
    - .planning/phases/00-platform-spike/00-SPIKE-FINDINGS.md exists and contains the literal headings `## Verdict`, `## Per-Criterion Results`, `## Actually Resolved Package Versions`, `## Errors Encountered and Fixes`, `## Fallback Recommendation`
    - The Verdict section contains exactly one of the literal strings `GO`, `GO-WITH-CAVEATS`, or `NO-GO`
    - The Per-Criterion Results table has 4 data rows, one per FND-01 through FND-04
    - Every version number in the "Actually Resolved" columns is copied verbatim from pubspec.lock (not from CLAUDE.md's claimed table) — spot-checkable by grepping both files for the same package name and comparing
    - If Verdict is not `GO`, the Fallback Recommendation section is non-empty and references at least one of the three documented fallback options
  </acceptance_criteria>
  <verify>
    <automated>grep -c "^## Verdict" .planning/phases/00-platform-spike/00-SPIKE-FINDINGS.md && grep -cE "^\| FND-0[1-4] \|" .planning/phases/00-platform-spike/00-SPIKE-FINDINGS.md</automated>
  </verify>
  <done>00-SPIKE-FINDINGS.md exists with all 5 required sections, a definitive verdict, per-criterion evidence sourced from captured logs (not assumptions), and actually-resolved versions from pubspec.lock.</done>
</task>

<task type="auto">
  <name>Task 2: Credential-hygiene pass before considering the phase closed</name>
  <files>spike_platform/.gitignore</files>
  <read_first>
    - spike_platform/lib/firebase_options_spike.dart (Plan 01 output — must contain only the dummy `demo-api-key` value)
    - spike_platform/.gitignore (as generated by `flutter create` in Plan 01 — confirm current contents before appending)
  </read_first>
  <action>
    Append these exact patterns to spike_platform/.gitignore (only if not already present): `firebase-debug.log`, `firestore-debug.log`, `ui-debug.log`, `*-debug.log`, `firebase_emulator_windows.log`, `firebase_emulator_android.log` — these emulator debug logs can be more verbose than the app-level run logs and are excluded from git while windows_run_log.txt/android_run_log.txt (the actual spike evidence) remain tracked. Then grep the entire spike_platform/ directory (lib/, firebase.json, firestore.rules) for the real Google API key pattern `AIza[0-9A-Za-z_-]{35}` and confirm zero matches. Separately grep spike_platform/lib/firebase_options_spike.dart for the literal string `demo-api-key` and confirm it appears exactly once, confirming no real credential replaced the dummy placeholder at any point during Plans 02-04.
  </action>
  <acceptance_criteria>
    - `grep -rE "AIza[0-9A-Za-z_-]{35}" spike_platform/` returns zero matches across the whole spike_platform/ tree
    - `grep -c "demo-api-key" spike_platform/lib/firebase_options_spike.dart` returns exactly 1
    - spike_platform/.gitignore contains `firebase-debug.log` and `*-debug.log`
  </acceptance_criteria>
  <verify>
    <automated>grep -rE "AIza[0-9A-Za-z_-]{35}" spike_platform/ ; echo "exit=$?"</automated>
  </verify>
  <done>No real Firebase API key pattern exists anywhere in spike_platform/, the dummy placeholder is confirmed unchanged, and verbose emulator debug logs are excluded from git.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| .planning/phases/00-platform-spike/00-SPIKE-FINDINGS.md ↔ git | Committed and shared with all 5 developers; must not leak any secret captured in a run log |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|------------------|
| T-00-11 | Information Disclosure | Findings doc / run logs committed to shared repo | mitigate | Task 2 greps the entire spike_platform/ tree for the real Google API key pattern (`AIza...`) and requires zero matches before the phase is considered done; verbose emulator debug logs excluded via .gitignore |
| T-00-SC (carried from Plan 01) | Tampering | Supply chain (firebase-tools, pub packages) | mitigate | Reaffirmed here as closed: no install occurred without the Plan 01 Task 3 legitimacy checkpoint |

</threat_model>

<verification>
- 00-SPIKE-FINDINGS.md exists with a definitive verdict and evidence-backed per-criterion table
- No real Firebase API key pattern exists anywhere in spike_platform/
- Emulator debug logs are gitignored; app-level run logs (the actual evidence) remain tracked
</verification>

<success_criteria>
Phase 0 is complete when 00-SPIKE-FINDINGS.md gives Phase 1 an unambiguous GO / GO-WITH-CAVEATS / NO-GO answer backed by captured runtime evidence on both platforms, with no real credentials committed anywhere in the throwaway spike.
</success_criteria>

<output>
Create `.planning/phases/00-platform-spike/00-05-SUMMARY.md` when done
</output>
