name: flashly_server
description: Dart REST backend for the Flashly application.
publish_to: none
version: 1.0.0

environment:
  sdk: ^3.11.1

dependencies:
  firebase_admin_sdk: ^0.5.3
  shelf: ^1.4.2
  shelf_router: ^1.1.4
  sqlite3: ^3.4.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.0
